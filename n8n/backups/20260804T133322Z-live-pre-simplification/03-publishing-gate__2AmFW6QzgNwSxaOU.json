{"updatedAt":"2026-08-03T11:16:05.666Z","createdAt":"2026-08-01T16:46:22.092Z","id":"2AmFW6QzgNwSxaOU","name":"03 — Publishing Gate","description":null,"active":true,"isArchived":false,"nodes":[{"parameters":{},"id":"d0fff759-482f-40f7-a813-f57a9a7d6214","name":"Called with a classified item","type":"n8n-nodes-base.executeWorkflowTrigger","typeVersion":1,"position":[-1328,192]},{"parameters":{"operation":"getAll","tableId":"sources","returnAll":true,"filters":{"conditions":[{"keyName":"id","condition":"eq","keyValue":"={{ $('Called with a classified item').first().json.item.source_id }}"}]}},"id":"932d0397-2051-44b8-aa6b-45726aa6b0c6","name":"Re-read source","type":"n8n-nodes-base.supabase","typeVersion":1,"position":[-1104,192],"alwaysOutputData":true,"credentials":{"supabaseApi":{"id":"10zDIxzQYFnJuOmh","name":"Supabase account 2"}}},{"parameters":{"operation":"getAll","tableId":"legal_updates","returnAll":true,"filters":{"conditions":[{"keyName":"content_hash","condition":"eq","keyValue":"={{ $('Called with a classified item').first().json.item.content_hash }}"}]}},"id":"aca14af5-ce2f-43ac-be52-80a3297353e3","name":"Look up hash","type":"n8n-nodes-base.supabase","typeVersion":1,"position":[-880,192],"alwaysOutputData":true,"credentials":{"supabaseApi":{"id":"10zDIxzQYFnJuOmh","name":"Supabase account 2"}}},{"parameters":{"jsCode":"// THE PUBLISHING GATE — the five rules, in one place.\n//\n// This sub-workflow is the ONLY code that decides whether something enters the\n// archive. Scheduled ingestion, manual runs (M10) and any future backfill all\n// call it, so the rules cannot drift between callers.\n//\n// The database enforces none of this. It stores what it is given and rejects\n// only what is structurally impossible (a duplicate hash, a confidence outside\n// 0..1). Every judgement below is n8n's.\n\nconst input = $('Called with a classified item').first().json;\nconst item = input.item;\nconst ai = input.ai;\nconst threshold = input.confidence_threshold;\n\nconst reject0 = (reason, detail) => [{\n  json: { published: false, reason, detail: detail ?? null, source_id: item?.source_id ?? null, content_hash: item?.content_hash ?? null },\n}];\n\n// Rejected upstream during AI-response validation (malformed/incomplete JSON,\n// invalid enum, etc). The real reason belongs to that stage, not to the five\n// rules below -- re-deriving a gate rejection here would both discard the\n// true cause and, since `ai` is null for these items, misreport it as a\n// missing confidence threshold.\nif (input.__rejected) return reject0(input.reason || 'ai_validation_failed', input.detail);\n\nconst sourceRows = $('Re-read source').all().map(i => i.json).filter(r => r && r.id);\nconst hashRows = $('Look up hash').all().map(i => i.json).filter(r => r && r.id);\n\nconst reject = (reason, detail) => [{\n  json: { published: false, reason, detail: detail ?? null, source_id: item.source_id, content_hash: item.content_hash },\n}];\n\n// ── Rule 0: the threshold itself must be present and sane ──────────────────\n// ai.confidence_threshold is a FAIL-CLOSED setting. If it is missing or\n// malformed the gate rejects everything rather than assuming a value, because\n// assuming one could quietly widen what publishes.\nif (typeof threshold !== 'number' || !Number.isFinite(threshold) || threshold < 0 || threshold > 1) {\n  return reject('missing_confidence_threshold', String(threshold));\n}\n\n// ── Rule 1: the source is registered, active and verified ──────────────────\n// Re-read at publish time, not trusted from the crawl: an admin may have\n// deactivated or un-verified the source while this item was in flight.\nconst source = sourceRows[0];\nif (!source) return reject('unknown_source');\nif (!source.active) return reject('inactive_source', source.authority_ar);\nif (source.config_status !== 'verified') return reject('unverified_source', source.config_status);\n\n// ── Rule 2: the item's URL is on the source's allow-list ───────────────────\n// Hostname extracted without the URL class, because this n8n environment does\n// not expose it inside Code nodes.\nconst urlMatch = String(item.source_url || '').match(/^(https?):\\/\\/([^/:?#]+)/i);\nif (!urlMatch) return reject('malformed_url', item.source_url);\nconst scheme = urlMatch[1].toLowerCase();\nif (scheme !== 'https' && scheme !== 'http') return reject('bad_url_scheme', scheme);\nconst host = urlMatch[2].toLowerCase();\nconst allowed = (source.allowed_domains || []).map(d => String(d).toLowerCase());\n// A discovery pseudo-source's own allow-list (news.google.com) has nothing to\n// do with the real-world domain a discovered item resolved to — that domain\n// was either matched against a REAL official source already (in which case\n// it IS that source's allowed_domains and this check would pass anyway), or\n// it wasn't matched and stays origin_type='discovery' on purpose. The static\n// allow-list is the right defence for a site n8n crawls directly; it is the\n// wrong tool for content a discovery engine surfaced from anywhere on the\n// web, which is what AI classification + this same gate's other four rules\n// exist to police instead.\nconst isDiscoverySource = source.ingestion_mode === 'discovery';\nif (!isDiscoverySource && !allowed.includes(host)) return reject('domain_mismatch', host);\n\n// ── Rule 3: not already published ──────────────────────────────────────────\n// The UNIQUE constraint is the backstop for the concurrent race; this is the\n// ordinary path, and a duplicate is a skip, never an error.\nif (hashRows.length > 0) return reject('duplicate', item.content_hash);\n\n// ── Rule 4: confidence clears the threshold ────────────────────────────────\nif (typeof ai.confidence !== 'number' || ai.confidence < threshold) {\n  return reject('low_confidence', String(ai.confidence));\n}\n\n// ── Rule 5: it is actually a legal update ──────────────────────────────────\nif (ai.is_legal_update !== true) return reject('not_legal_update');\n\n// ── Publication date: taken, never invented ────────────────────────────────\n// The crawler's date wins; the AI's is used only when the crawler found none.\n// If neither exists the item is rejected rather than stamped with the fetch\n// time, which would put a fabricated legal date in the archive.\nconst publicationDate = item.publication_date || ai.publication_date || null;\nif (!publicationDate) return reject('no_publication_date');\n\n// ── Country: the registry is authoritative ─────────────────────────────────\n// The source's country is used unless the AI identified a GCC-wide instrument,\n// which legitimately appears on national sites.\nconst country = ai.country === 'GCC' ? 'GCC' : source.country;\n\nreturn [{\n  json: {\n    published: true,\n    row: {\n      source_id: source.id,\n      content_hash: item.content_hash,\n      source_url: item.source_url,\n      title_ar: ai.title_ar,\n      summary_ar: ai.summary_ar,\n      country,\n      category: ai.category,\n      document_type: ai.document_type,\n      legal_status: ai.legal_status,\n      is_legal_update: true,\n      confidence: ai.confidence,\n      effective_date: ai.effective_date || null,\n      publication_date: publicationDate,\n      affected_entities: ai.affected_entities || [],\n      keywords: ai.keywords || [],\n      raw_excerpt: item.raw_excerpt,\n      document_path: item.document_path || null,\n      ai_model: item.ai_model,\n      origin_type: item.origin_type || 'official',\n      canonical_url: item.canonical_url || null,\n      discovery_engine: item.discovery_engine || null,\n    },\n  },\n}];"},"id":"1d9f238e-3f61-4a2b-8d50-063fb9c40e5c","name":"Apply the five rules","type":"n8n-nodes-base.code","typeVersion":2,"position":[-656,192]},{"parameters":{"conditions":{"options":{"caseSensitive":true,"version":2},"conditions":[{"leftValue":"={{ $json.published }}","rightValue":true,"operator":{"type":"boolean","operation":"true","singleValue":true}}],"combinator":"and"},"options":{}},"id":"e530e1cc-cce4-4515-a9d8-8d952e63a83f","name":"Passed?","type":"n8n-nodes-base.if","typeVersion":2.2,"position":[-448,192]},{"parameters":{"tableId":"legal_updates","dataToSend":"autoMapInputData"},"id":"88274bc9-a6cf-428b-93db-3733b50a4af1","name":"Insert into archive","type":"n8n-nodes-base.supabase","typeVersion":1,"position":[-224,80],"credentials":{"supabaseApi":{"id":"10zDIxzQYFnJuOmh","name":"Supabase account 2"}},"onError":"continueErrorOutput"},{"parameters":{"jsCode":"// Insert succeeded.\nconst row = $('Apply the five rules').first().json.row;\nreturn [{ json: { outcome: 'published', source_id: row.source_id, content_hash: row.content_hash } }];"},"id":"806177dd-e55f-4885-a260-efada5899842","name":"Published","type":"n8n-nodes-base.code","typeVersion":2,"position":[0,0]},{"parameters":{"jsCode":"// Insert failed.\n//\n// A unique-violation here is the concurrent-execution race the constraint\n// exists for: two runs classified the same item at once and both passed the\n// gate's hash lookup. That is a DUPLICATE, not a failure — classifying it as\n// an error would make a healthy source look broken.\n\nconst gate = $('Apply the five rules').first().json;\nconst err = $input.first().json.error ?? $input.first().json;\nconst message = String(err?.message ?? err?.details ?? err ?? 'insert failed');\n\nconst isDuplicate = /duplicate key|content_hash_unique|23505/i.test(message);\n\nreturn [{\n  json: {\n    outcome: isDuplicate ? 'duplicate' : 'insert_failed',\n    source_id: gate.row?.source_id ?? null,\n    content_hash: gate.row?.content_hash ?? null,\n    error_message: isDuplicate ? null : message.slice(0, 1000),\n  },\n}];"},"id":"762191f3-d40e-495f-89e8-3c1c38bccd79","name":"Insert failed","type":"n8n-nodes-base.code","typeVersion":2,"position":[0,160]},{"parameters":{"jsCode":"// Rejected by the gate. Not an error — a decision, carried back to the caller\n// so it can be counted in workflow_logs.rejection_reasons.\nconst g = $input.first().json;\nreturn [{ json: { outcome: 'rejected', reason: g.reason, detail: g.detail, source_id: g.source_id } }];"},"id":"4888edd8-d54e-47dc-a028-098ef3d2eb14","name":"Rejected","type":"n8n-nodes-base.code","typeVersion":2,"position":[-224,304]},{"parameters":{},"type":"n8n-nodes-base.stickyNote","position":[-1216,-80],"typeVersion":1,"id":"ea564e95-4bec-4b74-afdc-819ae0eedcdc","name":"Sticky Note"},{"parameters":{},"type":"n8n-nodes-base.stickyNote","position":[-928,-80],"typeVersion":1,"id":"a6eac944-4d68-4ff4-8036-4c8006cd8255","name":"Sticky Note1"}],"connections":{"Called with a classified item":{"main":[[{"node":"Re-read source","type":"main","index":0}]]},"Re-read source":{"main":[[{"node":"Look up hash","type":"main","index":0}]]},"Look up hash":{"main":[[{"node":"Apply the five rules","type":"main","index":0}]]},"Apply the five rules":{"main":[[{"node":"Passed?","type":"main","index":0}]]},"Passed?":{"main":[[{"node":"Insert into archive","type":"main","index":0}],[{"node":"Rejected","type":"main","index":0}]]},"Insert into archive":{"main":[[{"node":"Published","type":"main","index":0}],[{"node":"Insert failed","type":"main","index":0}]]}},"settings":{"executionOrder":"v1","binaryMode":"separate","availableInMCP":false,"callerPolicy":"workflowsFromSameOwner"},"staticData":null,"meta":{"templateCredsSetupCompleted":true},"pinData":{},"versionId":"39d0d6f4-32c7-4d29-87f3-730079e572f5","activeVersionId":"39d0d6f4-32c7-4d29-87f3-730079e572f5","versionCounter":19,"triggerCount":0,"shared":[{"updatedAt":"2026-08-01T16:46:22.092Z","createdAt":"2026-08-01T16:46:22.092Z","role":"workflow:owner","workflowId":"2AmFW6QzgNwSxaOU","projectId":"Fo19xH6yJI9Yypvs","project":{"updatedAt":"2026-06-03T20:11:11.632Z","createdAt":"2026-06-03T19:16:50.624Z","id":"Fo19xH6yJI9Yypvs","name":"Asayel Qaid <asayelmq@gmail.com>","type":"personal","icon":null,"description":null,"creatorId":"c6441430-2935-43cb-a7bd-db0da8ae18ce","projectRelations":[{"updatedAt":"2026-06-03T19:16:50.624Z","createdAt":"2026-06-03T19:16:50.624Z","userId":"c6441430-2935-43cb-a7bd-db0da8ae18ce","projectId":"Fo19xH6yJI9Yypvs","user":{"updatedAt":"2026-08-04T10:19:51.021Z","createdAt":"2026-06-03T19:16:50.624Z","id":"c6441430-2935-43cb-a7bd-db0da8ae18ce","email":"asayelmq@gmail.com","firstName":"Asayel","lastName":"Qaid","personalizationAnswers":{"version":"v4","personalization_survey_submitted_at":"2026-06-03T20:12:25.559Z","personalization_survey_n8n_version":"2.7.4","automationGoalDevops":["reporting","ticketing-systems-integrations"],"companySize":"20-99","companyType":"systems-integrator","role":"it","reportedSource":"friend"},"settings":{"easyAIWorkflowOnboarded":true,"firstSuccessfulWorkflowId":"FHt8uKbBcixIWbWO","userActivated":true,"userActivatedAt":1785650091040},"disabled":false,"mfaEnabled":false,"lastActiveAt":"2026-08-03","isPending":false}}]}}],"tags":[{"updatedAt":"2026-08-01T15:13:31.517Z","createdAt":"2026-08-01T15:13:31.517Z","id":"CFx6g4HIqhyJJh7g","name":"legal-intelligence"}],"activeVersion":{"updatedAt":"2026-08-03T11:16:05.668Z","createdAt":"2026-08-03T11:16:05.668Z","versionId":"39d0d6f4-32c7-4d29-87f3-730079e572f5","workflowId":"2AmFW6QzgNwSxaOU","nodes":[{"parameters":{},"id":"d0fff759-482f-40f7-a813-f57a9a7d6214","name":"Called with a classified item","type":"n8n-nodes-base.executeWorkflowTrigger","typeVersion":1,"position":[-1328,192]},{"parameters":{"operation":"getAll","tableId":"sources","returnAll":true,"filters":{"conditions":[{"keyName":"id","condition":"eq","keyValue":"={{ $('Called with a classified item').first().json.item.source_id }}"}]}},"id":"932d0397-2051-44b8-aa6b-45726aa6b0c6","name":"Re-read source","type":"n8n-nodes-base.supabase","typeVersion":1,"position":[-1104,192],"alwaysOutputData":true,"credentials":{"supabaseApi":{"id":"10zDIxzQYFnJuOmh","name":"Supabase account 2"}}},{"parameters":{"operation":"getAll","tableId":"legal_updates","returnAll":true,"filters":{"conditions":[{"keyName":"content_hash","condition":"eq","keyValue":"={{ $('Called with a classified item').first().json.item.content_hash }}"}]}},"id":"aca14af5-ce2f-43ac-be52-80a3297353e3","name":"Look up hash","type":"n8n-nodes-base.supabase","typeVersion":1,"position":[-880,192],"alwaysOutputData":true,"credentials":{"supabaseApi":{"id":"10zDIxzQYFnJuOmh","name":"Supabase account 2"}}},{"parameters":{"jsCode":"// THE PUBLISHING GATE — the five rules, in one place.\n//\n// This sub-workflow is the ONLY code that decides whether something enters the\n// archive. Scheduled ingestion, manual runs (M10) and any future backfill all\n// call it, so the rules cannot drift between callers.\n//\n// The database enforces none of this. It stores what it is given and rejects\n// only what is structurally impossible (a duplicate hash, a confidence outside\n// 0..1). Every judgement below is n8n's.\n\nconst input = $('Called with a classified item').first().json;\nconst item = input.item;\nconst ai = input.ai;\nconst threshold = input.confidence_threshold;\n\nconst reject0 = (reason, detail) => [{\n  json: { published: false, reason, detail: detail ?? null, source_id: item?.source_id ?? null, content_hash: item?.content_hash ?? null },\n}];\n\n// Rejected upstream during AI-response validation (malformed/incomplete JSON,\n// invalid enum, etc). The real reason belongs to that stage, not to the five\n// rules below -- re-deriving a gate rejection here would both discard the\n// true cause and, since `ai` is null for these items, misreport it as a\n// missing confidence threshold.\nif (input.__rejected) return reject0(input.reason || 'ai_validation_failed', input.detail);\n\nconst sourceRows = $('Re-read source').all().map(i => i.json).filter(r => r && r.id);\nconst hashRows = $('Look up hash').all().map(i => i.json).filter(r => r && r.id);\n\nconst reject = (reason, detail) => [{\n  json: { published: false, reason, detail: detail ?? null, source_id: item.source_id, content_hash: item.content_hash },\n}];\n\n// ── Rule 0: the threshold itself must be present and sane ──────────────────\n// ai.confidence_threshold is a FAIL-CLOSED setting. If it is missing or\n// malformed the gate rejects everything rather than assuming a value, because\n// assuming one could quietly widen what publishes.\nif (typeof threshold !== 'number' || !Number.isFinite(threshold) || threshold < 0 || threshold > 1) {\n  return reject('missing_confidence_threshold', String(threshold));\n}\n\n// ── Rule 1: the source is registered, active and verified ──────────────────\n// Re-read at publish time, not trusted from the crawl: an admin may have\n// deactivated or un-verified the source while this item was in flight.\nconst source = sourceRows[0];\nif (!source) return reject('unknown_source');\nif (!source.active) return reject('inactive_source', source.authority_ar);\nif (source.config_status !== 'verified') return reject('unverified_source', source.config_status);\n\n// ── Rule 2: the item's URL is on the source's allow-list ───────────────────\n// Hostname extracted without the URL class, because this n8n environment does\n// not expose it inside Code nodes.\nconst urlMatch = String(item.source_url || '').match(/^(https?):\\/\\/([^/:?#]+)/i);\nif (!urlMatch) return reject('malformed_url', item.source_url);\nconst scheme = urlMatch[1].toLowerCase();\nif (scheme !== 'https' && scheme !== 'http') return reject('bad_url_scheme', scheme);\nconst host = urlMatch[2].toLowerCase();\nconst allowed = (source.allowed_domains || []).map(d => String(d).toLowerCase());\n// A discovery pseudo-source's own allow-list (news.google.com) has nothing to\n// do with the real-world domain a discovered item resolved to — that domain\n// was either matched against a REAL official source already (in which case\n// it IS that source's allowed_domains and this check would pass anyway), or\n// it wasn't matched and stays origin_type='discovery' on purpose. The static\n// allow-list is the right defence for a site n8n crawls directly; it is the\n// wrong tool for content a discovery engine surfaced from anywhere on the\n// web, which is what AI classification + this same gate's other four rules\n// exist to police instead.\nconst isDiscoverySource = source.ingestion_mode === 'discovery';\nif (!isDiscoverySource && !allowed.includes(host)) return reject('domain_mismatch', host);\n\n// ── Rule 3: not already published ──────────────────────────────────────────\n// The UNIQUE constraint is the backstop for the concurrent race; this is the\n// ordinary path, and a duplicate is a skip, never an error.\nif (hashRows.length > 0) return reject('duplicate', item.content_hash);\n\n// ── Rule 4: confidence clears the threshold ────────────────────────────────\nif (typeof ai.confidence !== 'number' || ai.confidence < threshold) {\n  return reject('low_confidence', String(ai.confidence));\n}\n\n// ── Rule 5: it is actually a legal update ──────────────────────────────────\nif (ai.is_legal_update !== true) return reject('not_legal_update');\n\n// ── Publication date: taken, never invented ────────────────────────────────\n// The crawler's date wins; the AI's is used only when the crawler found none.\n// If neither exists the item is rejected rather than stamped with the fetch\n// time, which would put a fabricated legal date in the archive.\nconst publicationDate = item.publication_date || ai.publication_date || null;\nif (!publicationDate) return reject('no_publication_date');\n\n// ── Country: the registry is authoritative ─────────────────────────────────\n// The source's country is used unless the AI identified a GCC-wide instrument,\n// which legitimately appears on national sites.\nconst country = ai.country === 'GCC' ? 'GCC' : source.country;\n\nreturn [{\n  json: {\n    published: true,\n    row: {\n      source_id: source.id,\n      content_hash: item.content_hash,\n      source_url: item.source_url,\n      title_ar: ai.title_ar,\n      summary_ar: ai.summary_ar,\n      country,\n      category: ai.category,\n      document_type: ai.document_type,\n      legal_status: ai.legal_status,\n      is_legal_update: true,\n      confidence: ai.confidence,\n      effective_date: ai.effective_date || null,\n      publication_date: publicationDate,\n      affected_entities: ai.affected_entities || [],\n      keywords: ai.keywords || [],\n      raw_excerpt: item.raw_excerpt,\n      document_path: item.document_path || null,\n      ai_model: item.ai_model,\n      origin_type: item.origin_type || 'official',\n      canonical_url: item.canonical_url || null,\n      discovery_engine: item.discovery_engine || null,\n    },\n  },\n}];"},"id":"1d9f238e-3f61-4a2b-8d50-063fb9c40e5c","name":"Apply the five rules","type":"n8n-nodes-base.code","typeVersion":2,"position":[-656,192]},{"parameters":{"conditions":{"options":{"caseSensitive":true,"version":2},"conditions":[{"leftValue":"={{ $json.published }}","rightValue":true,"operator":{"type":"boolean","operation":"true","singleValue":true}}],"combinator":"and"},"options":{}},"id":"e530e1cc-cce4-4515-a9d8-8d952e63a83f","name":"Passed?","type":"n8n-nodes-base.if","typeVersion":2.2,"position":[-448,192]},{"parameters":{"tableId":"legal_updates","dataToSend":"autoMapInputData"},"id":"88274bc9-a6cf-428b-93db-3733b50a4af1","name":"Insert into archive","type":"n8n-nodes-base.supabase","typeVersion":1,"position":[-224,80],"credentials":{"supabaseApi":{"id":"10zDIxzQYFnJuOmh","name":"Supabase account 2"}},"onError":"continueErrorOutput"},{"parameters":{"jsCode":"// Insert succeeded.\nconst row = $('Apply the five rules').first().json.row;\nreturn [{ json: { outcome: 'published', source_id: row.source_id, content_hash: row.content_hash } }];"},"id":"806177dd-e55f-4885-a260-efada5899842","name":"Published","type":"n8n-nodes-base.code","typeVersion":2,"position":[0,0]},{"parameters":{"jsCode":"// Insert failed.\n//\n// A unique-violation here is the concurrent-execution race the constraint\n// exists for: two runs classified the same item at once and both passed the\n// gate's hash lookup. That is a DUPLICATE, not a failure — classifying it as\n// an error would make a healthy source look broken.\n\nconst gate = $('Apply the five rules').first().json;\nconst err = $input.first().json.error ?? $input.first().json;\nconst message = String(err?.message ?? err?.details ?? err ?? 'insert failed');\n\nconst isDuplicate = /duplicate key|content_hash_unique|23505/i.test(message);\n\nreturn [{\n  json: {\n    outcome: isDuplicate ? 'duplicate' : 'insert_failed',\n    source_id: gate.row?.source_id ?? null,\n    content_hash: gate.row?.content_hash ?? null,\n    error_message: isDuplicate ? null : message.slice(0, 1000),\n  },\n}];"},"id":"762191f3-d40e-495f-89e8-3c1c38bccd79","name":"Insert failed","type":"n8n-nodes-base.code","typeVersion":2,"position":[0,160]},{"parameters":{"jsCode":"// Rejected by the gate. Not an error — a decision, carried back to the caller\n// so it can be counted in workflow_logs.rejection_reasons.\nconst g = $input.first().json;\nreturn [{ json: { outcome: 'rejected', reason: g.reason, detail: g.detail, source_id: g.source_id } }];"},"id":"4888edd8-d54e-47dc-a028-098ef3d2eb14","name":"Rejected","type":"n8n-nodes-base.code","typeVersion":2,"position":[-224,304]},{"parameters":{},"type":"n8n-nodes-base.stickyNote","position":[-1216,-80],"typeVersion":1,"id":"ea564e95-4bec-4b74-afdc-819ae0eedcdc","name":"Sticky Note"},{"parameters":{},"type":"n8n-nodes-base.stickyNote","position":[-928,-80],"typeVersion":1,"id":"a6eac944-4d68-4ff4-8036-4c8006cd8255","name":"Sticky Note1"}],"connections":{"Called with a classified item":{"main":[[{"node":"Re-read source","type":"main","index":0}]]},"Re-read source":{"main":[[{"node":"Look up hash","type":"main","index":0}]]},"Look up hash":{"main":[[{"node":"Apply the five rules","type":"main","index":0}]]},"Apply the five rules":{"main":[[{"node":"Passed?","type":"main","index":0}]]},"Passed?":{"main":[[{"node":"Insert into archive","type":"main","index":0}],[{"node":"Rejected","type":"main","index":0}]]},"Insert into archive":{"main":[[{"node":"Published","type":"main","index":0}],[{"node":"Insert failed","type":"main","index":0}]]}},"authors":"Asayel Qaid","name":null,"description":null,"autosaved":false,"workflowPublishHistory":[{"createdAt":"2026-08-03T11:16:05.731Z","id":446,"workflowId":"2AmFW6QzgNwSxaOU","versionId":"39d0d6f4-32c7-4d29-87f3-730079e572f5","event":"activated","userId":"c6441430-2935-43cb-a7bd-db0da8ae18ce"}]}}