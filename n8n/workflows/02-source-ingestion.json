{
  "updatedAt": "2026-08-02T12:25:02.734Z",
  "createdAt": "2026-08-01T16:46:39.922Z",
  "id": "FHt8uKbBcixIWbWO",
  "name": "02 — Source Ingestion",
  "description": null,
  "active": true,
  "isArchived": false,
  "nodes": [
    {
      "parameters": {},
      "id": "9858366b-8ef5-4bd8-b4a4-107e49c5436f",
      "name": "Called by scheduler",
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1,
      "position": [
        -480,
        304
      ]
    },
    {
      "parameters": {
        "rules": {
          "values": [
            {
              "conditions": {
                "options": {
                  "caseSensitive": true,
                  "version": 2
                },
                "conditions": [
                  {
                    "leftValue": "={{ $json.source.parser_type }}",
                    "rightValue": "rss",
                    "operator": {
                      "type": "string",
                      "operation": "equals"
                    }
                  }
                ],
                "combinator": "and"
              }
            },
            {
              "conditions": {
                "options": {
                  "caseSensitive": true,
                  "version": 2
                },
                "conditions": [
                  {
                    "leftValue": "={{ $json.source.parser_type }}",
                    "rightValue": "api",
                    "operator": {
                      "type": "string",
                      "operation": "equals"
                    }
                  }
                ],
                "combinator": "and"
              }
            },
            {
              "conditions": {
                "options": {
                  "caseSensitive": true,
                  "version": 2
                },
                "conditions": [
                  {
                    "leftValue": "={{ $json.source.parser_type }}",
                    "rightValue": "html",
                    "operator": {
                      "type": "string",
                      "operation": "equals"
                    }
                  }
                ],
                "combinator": "and"
              }
            },
            {
              "conditions": {
                "options": {
                  "caseSensitive": true,
                  "version": 2
                },
                "conditions": [
                  {
                    "leftValue": "={{ $json.source.parser_type }}",
                    "rightValue": "pdf",
                    "operator": {
                      "type": "string",
                      "operation": "equals"
                    }
                  }
                ],
                "combinator": "and"
              }
            }
          ]
        },
        "options": {
          "fallbackOutput": "none"
        }
      },
      "id": "53c083b9-d01b-41f7-a19a-9d0664cd5359",
      "name": "Parser router",
      "type": "n8n-nodes-base.switch",
      "typeVersion": 3.2,
      "position": [
        -256,
        304
      ]
    },
    {
      "parameters": {
        "url": "={{ $json.source.feed_url }}",
        "options": {
          "response": {
            "response": {
              "responseFormat": "text"
            }
          },
          "timeout": 30000
        }
      },
      "id": "79792d0e-bcaf-4ab9-9147-08f3ef970407",
      "name": "Fetch RSS",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        0,
        0
      ],
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 2000,
      "onError": "continueErrorOutput"
    },
    {
      "parameters": {
        "options": {}
      },
      "id": "421b3d1c-9b4b-4f2e-8de4-863036d46c30",
      "name": "Parse XML",
      "type": "n8n-nodes-base.xml",
      "typeVersion": 1,
      "position": [
        224,
        0
      ]
    },
    {
      "parameters": {
        "jsCode": "// RSS lane → RawItem[]\n//\n// parser_config may name which fields carry the date and body; both default\n// to the RSS conventions. Nothing here is guessed at crawl time — an\n// unconfigured source never reaches this workflow, because it cannot be\n// activated.\n\nconst src = $('Called by scheduler').first().json.source;\nconst cfg = src.parser_config || {};\nconst dateField = cfg.date_field || 'pubDate';\nconst contentField = cfg.content_field || 'description';\n\nconst root = $input.first().json;\nconst channel = root?.rss?.channel ?? root?.feed ?? {};\nlet entries = channel.item ?? channel.entry ?? root?.feed?.entry ?? [];\nif (!Array.isArray(entries)) entries = [entries];\n\nconst text = (v) => {\n  if (v === null || v === undefined) return '';\n  if (typeof v === 'string') return v;\n  if (typeof v === 'object') return v._ ?? v['#text'] ?? '';\n  return String(v);\n};\n\nconst toIsoDate = (v) => {\n  const raw = text(v);\n  if (!raw) return null;\n  const d = new Date(raw);\n  // An unparseable date becomes null. The AI is instructed never to invent\n  // one, and neither does this.\n  return Number.isNaN(d.getTime()) ? null : d.toISOString().slice(0, 10);\n};\n\nconst linkOf = (e) => {\n  if (typeof e.link === 'string') return e.link;\n  if (Array.isArray(e.link)) return e.link[0]?.$?.href ?? text(e.link[0]);\n  return e.link?.$?.href ?? text(e.link) ?? e.guid ? text(e.guid) : '';\n};\n\nreturn entries.filter(Boolean).map((e) => ({\n  json: {\n    source_id: src.id,\n    source_url: linkOf(e),\n    title_raw: text(e.title),\n    content_raw: text(e[contentField]) || text(e.summary) || text(e.content),\n    publication_date: toIsoDate(e[dateField] ?? e.published ?? e.updated),\n    attachments: [],\n    fetched_at: new Date().toISOString(),\n  },\n}));"
      },
      "id": "40ab11fd-8caa-4050-a276-25af3ffbdbf9",
      "name": "RSS → RawItem",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        448,
        0
      ]
    },
    {
      "parameters": {
        "url": "={{ $json.source.feed_url }}",
        "sendHeaders": true,
        "specifyHeaders": "json",
        "jsonHeaders": "={{ JSON.stringify($json.source.parser_config.headers || {}) }}",
        "options": {
          "timeout": 30000
        }
      },
      "id": "3d0cd50a-feef-41f2-a744-20d39b4cb093",
      "name": "Fetch API",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        0,
        208
      ],
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 2000,
      "onError": "continueErrorOutput"
    },
    {
      "parameters": {
        "jsCode": "// API lane → RawItem[]\n// Field paths come from parser_config, filled in by an admin who inspected\n// the real response. Nothing is inferred.\n\nconst src = $('Called by scheduler').first().json.source;\nconst cfg = src.parser_config || {};\n\nconst dig = (obj, path) =>\n  (path || '').split('.').filter(Boolean).reduce((o, k) => (o == null ? undefined : o[k]), obj);\n\nconst payload = $input.first().json;\nlet items = cfg.items_path ? dig(payload, cfg.items_path) : payload;\nif (!Array.isArray(items)) items = items ? [items] : [];\n\nconst toIsoDate = (raw) => {\n  if (!raw) return null;\n  const d = new Date(raw);\n  return Number.isNaN(d.getTime()) ? null : d.toISOString().slice(0, 10);\n};\n\nreturn items.map((it) => ({\n  json: {\n    source_id: src.id,\n    source_url: String(dig(it, cfg.url) ?? ''),\n    title_raw: String(dig(it, cfg.title) ?? ''),\n    content_raw: String(dig(it, cfg.body) ?? ''),\n    publication_date: toIsoDate(dig(it, cfg.date)),\n    attachments: [],\n    fetched_at: new Date().toISOString(),\n  },\n}));"
      },
      "id": "cc84dd24-e6a7-4725-b99d-4d75a19900ed",
      "name": "API → RawItem",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        224,
        208
      ]
    },
    {
      "parameters": {
        "url": "={{ $json.source.base_url }}",
        "options": {
          "response": {
            "response": {
              "responseFormat": "text"
            }
          },
          "timeout": 30000
        }
      },
      "id": "537b9077-bd28-4f2c-8978-ba5ada3f26ba",
      "name": "Fetch HTML",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        0,
        400
      ],
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 2000,
      "onError": "continueErrorOutput"
    },
    {
      "parameters": {
        "operation": "extractHtmlContent",
        "extractionValues": {
          "values": [
            {
              "key": "items",
              "cssSelector": "={{ $('Called by scheduler').first().json.source.parser_config.list }}",
              "returnValue": "html",
              "returnArray": true
            }
          ]
        },
        "options": {}
      },
      "id": "be3851f6-129e-4606-9181-99e54393d774",
      "name": "Extract listing",
      "type": "n8n-nodes-base.html",
      "typeVersion": 1.2,
      "position": [
        224,
        400
      ]
    },
    {
      "parameters": {
        "jsCode": "// HTML lane → RawItem[]\n//\n// Selectors come from parser_config, which an admin filled in against the\n// live page during verification. A source with an empty config cannot be\n// marked verified and therefore cannot be active, so this never runs on\n// guessed selectors.\n\nconst src = $('Called by scheduler').first().json.source;\nconst cfg = src.parser_config || {};\n\n// Extract origin without using the URL class,\n// because this n8n environment does not expose it inside Code nodes.\nconst baseUrl = String(src.base_url || '').replace(/\\/+$/, '');\nconst originMatch = baseUrl.match(/^(https?:\\/\\/[^/]+)/i);\nconst origin = originMatch ? originMatch[1] : baseUrl;\n\nconst blocks = $input.first().json.items || [];\n\n// Minimal extraction over the fragment HTML. Kept dependency-free: the\n// HTML node has already narrowed the document to the list items.\nconst pick = (html, selector) => {\n  if (!selector) return '';\n  const attrMatch = selector.match(/^(.*)@([\\w-]+)$/);\n  const sel = (attrMatch ? attrMatch[1] : selector).trim();\n  const attr = attrMatch ? attrMatch[2] : null;\n  const tag = sel.split(/[\\s>]/).pop().replace(/[.#].*$/, '') || 'a';\n  const re = new RegExp(`<${tag}[^>]*>([\\\\s\\\\S]*?)</${tag}>`, 'i');\n  if (attr) {\n    const attrRe = new RegExp(`<${tag}[^>]*\\\\s${attr}=[\"']([^\"']+)[\"']`, 'i');\n    const m = html.match(attrRe);\n    return m ? m[1] : '';\n  }\n  const m = html.match(re);\n  return m ? m[1].replace(/<[^>]+>/g, ' ').replace(/\\s+/g, ' ').trim() : '';\n};\n\nconst absolute = (href) => {\n  const value = String(href || '').trim();\n  if (!value) return '';\n  if (/^https?:\\/\\//i.test(value)) return value;\n  if (value.startsWith('//')) return `https:${value}`;\n  if (value.startsWith('/')) return `${origin}${value}`;\n  return `${baseUrl}/${value.replace(/^\\/+/, '')}`;\n};\n\nconst toIsoDate = (raw) => {\n  if (!raw) return null;\n  const d = new Date(raw);\n  return Number.isNaN(d.getTime()) ? null : d.toISOString().slice(0, 10);\n};\n\nreturn blocks.map((html) => {\n  const link = absolute(pick(html, cfg.link));\n  const pdfMatch = html.match(/href=[\"']([^\"']+\\.pdf)[\"']/i);\n  return {\n    json: {\n      source_id: src.id,\n      source_url: link,\n      title_raw: pick(html, cfg.title),\n      content_raw: pick(html, cfg.body),\n      publication_date: toIsoDate(pick(html, cfg.date)),\n      // PDF enrichment applies to every lane, not just parser_type = pdf:\n      // gazettes routinely publish a listing entry whose substance is an\n      // attached document.\n      attachments: pdfMatch ? [absolute(pdfMatch[1])] : [],\n      fetched_at: new Date().toISOString(),\n    },\n  };\n});\n"
      },
      "id": "66a54f22-2502-497c-94b1-ff5c01c7a351",
      "name": "HTML → RawItem",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        448,
        400
      ]
    },
    {
      "parameters": {
        "url": "={{ $json.source.base_url }}",
        "options": {
          "response": {
            "response": {
              "responseFormat": "text"
            }
          },
          "timeout": 30000
        }
      },
      "id": "e6cbeaaa-36b2-4d32-b352-ee4ce1adab5f",
      "name": "Fetch PDF index",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        0,
        608
      ],
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 2000,
      "onError": "continueErrorOutput"
    },
    {
      "parameters": {
        "jsCode": "const src = $('Called by scheduler').first().json.source;\nconst cfg = src.parser_config || {};\nconst maxPages = Number(cfg.max_pages) || 40;\n\nconst baseUrl = String(src.base_url || '').replace(/\\/+$/, '');\nconst originMatch = baseUrl.match(/^(https?:\\/\\/[^/]+)/i);\nconst origin = originMatch ? originMatch[1] : baseUrl;\n\nconst makeAbsolute = (href) => {\n  const value = String(href || '').trim();\n\n  if (!value) return '';\n  if (/^https?:\\/\\//i.test(value)) return value;\n  if (value.startsWith('//')) return `https:${value}`;\n  if (value.startsWith('/')) return `${origin}${value}`;\n\n  return `${baseUrl}/${value.replace(/^\\/+/, '')}`;\n};\n\nconst input = $input.first().json;\n\nconst html =\n  typeof input.data === 'string'\n    ? input.data\n    : typeof input.body === 'string'\n      ? input.body\n      : '';\n\nconst links = [...html.matchAll(\n  /href=[\"']([^\"']+\\.pdf(?:\\?[^\"']*)?)[\"'][^>]*>([\\s\\S]*?)</gi\n)]\n  .slice(0, maxPages)\n  .map((match) => ({\n    href: makeAbsolute(match[1]),\n    label: match[2]\n      .replace(/<[^>]+>/g, ' ')\n      .replace(/\\s+/g, ' ')\n      .trim(),\n  }))\n  .filter((link) => link.href);\n\nreturn links.map((link) => ({\n  json: {\n    source_id: src.id,\n    source_url: link.href,\n    title_raw: link.label || 'عدد من الجريدة الرسمية أم القرى',\n    content_raw: '',\n    publication_date: null,\n    attachments: [link.href],\n    fetched_at: new Date().toISOString(),\n  },\n}));"
      },
      "id": "7c4ba6b3-537b-4aff-8c0a-c728f7b1e896",
      "name": "PDF index → RawItem",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        224,
        608
      ]
    },
    {
      "parameters": {
        "numberInputs": 4
      },
      "id": "436ee566-779a-4d1e-89f6-90da3c920321",
      "name": "Merge lanes",
      "type": "n8n-nodes-base.merge",
      "typeVersion": 3,
      "position": [
        688,
        304
      ]
    },
    {
      "parameters": {
        "jsCode": "// Normalises and validates every RawItem before anything downstream sees it.\n//\n// This is the single contract between parsing and the M9 AI stage: whatever\n// lane produced an item, it leaves here in the same shape or not at all.\n\nconst src = $('Called by scheduler').first().json.source;\n\nconst allowed = (src.allowed_domains || [])\n  .map((domain) => String(domain).trim().toLowerCase())\n  .filter(Boolean);\n\nconst out = [];\nconst rejected = {\n  no_url: 0,\n  no_title: 0,\n  domain_mismatch: 0,\n};\n\nfor (const item of $input.all()) {\n  const r = item.json;\n\n  const sourceUrl = String(r.source_url || '').trim();\n  const titleRaw = String(r.title_raw || '').trim();\n\n  if (!sourceUrl) {\n    rejected.no_url += 1;\n    continue;\n  }\n\n  if (!titleRaw) {\n    rejected.no_title += 1;\n    continue;\n  }\n\n  // Extract hostname without using the URL class,\n  // because this n8n environment does not expose it inside Code nodes.\n  const hostMatch = sourceUrl.match(/^https?:\\/\\/([^/:?#]+)/i);\n\n  if (!hostMatch) {\n    rejected.no_url += 1;\n    continue;\n  }\n\n  const host = hostMatch[1].toLowerCase();\n\n  if (!allowed.includes(host)) {\n    rejected.domain_mismatch += 1;\n    continue;\n  }\n\n  out.push({\n    json: {\n      source_id: src.id,\n      source_url: sourceUrl,\n      title_raw: titleRaw.slice(0, 2000),\n      content_raw: String(r.content_raw || '').trim().slice(0, 50000),\n      publication_date: r.publication_date || null,\n      attachments: Array.isArray(r.attachments)\n        ? r.attachments.slice(0, 5)\n        : [],\n      fetched_at: r.fetched_at || new Date().toISOString(),\n\n      // Carried through so M9 can log against the correct source.\n      source: {\n        id: src.id,\n        country: src.country,\n        authority_ar: src.authority_ar,\n        allowed_domains: src.allowed_domains,\n      },\n    },\n  });\n}\n\nconsole.log(\n  `normalised ${out.length} items; rejected ${JSON.stringify(rejected)}`\n);\n\n// A genuinely empty result must still leave exactly one item behind, or every\n// downstream node (including the log write) is skipped by n8n's zero-item rule\n// and the run vanishes without a workflow_logs row or a webhook response.\nif (out.length === 0) {\n  return [{ json: { __empty: true, raw_count: $input.all().length, rejected } }];\n}\n\n// M9 attaches the AI stage and the Publishing Gate to this output.\nreturn out;"
      },
      "id": "871b4a68-729a-4d68-9886-b6e57016a7e3",
      "name": "Normalise RawItem",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        912,
        304
      ]
    },
    {
      "parameters": {
        "jsCode": "// Fetch failure for this source.\n//\n// Isolated on purpose: one unreachable source must not stop the others in the\n// same scheduler tick. M10 turns this into retry state and a health snapshot;\n// for now it records the failure and ends this source's run.\n\nconst src = $('Called by scheduler').first().json.source;\nconst err = $input.first().json.error || $input.first().json;\n\nreturn [{\n  json: {\n    failed: true,\n    source_id: src.id,\n    authority_ar: src.authority_ar,\n    error_message: String(err?.message || err?.reason || 'fetch failed').slice(0, 1000),\n  },\n}];"
      },
      "id": "785ba1b9-0edb-476e-bb8f-988f21151f97",
      "name": "Record fetch failure",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        448,
        800
      ]
    },
    {
      "parameters": {
        "operation": "getAll",
        "tableId": "app_settings",
        "returnAll": true
      },
      "id": "db447710-99b0-46f5-ad12-04dcaadf2010",
      "name": "Get threshold",
      "type": "n8n-nodes-base.supabase",
      "typeVersion": 1,
      "position": [
        912,
        528
      ],
      "alwaysOutputData": true,
      "credentials": {
        "supabaseApi": {
          "id": "10zDIxzQYFnJuOmh",
          "name": "Supabase account 2"
        }
      }
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://api.anthropic.com/v1/messages",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendHeaders": true,
        "specifyHeaders": "json",
        "jsonHeaders": "={{ JSON.stringify({ \"anthropic-version\": \"2023-06-01\", \"content-type\": \"application/json\" }) }}",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ model: 'claude-sonnet-5', max_tokens: 1500, temperature: 0, system: $('Classification prompt').first().json.system_prompt, messages: [{ role: 'user', content: `الجهة: ${$json.source.authority_ar}\\nالدولة المسجّلة للمصدر: ${$json.source.country}\\nالرابط: ${$json.source_url}\\nتاريخ النشر المستخرج من المصدر: ${$json.publication_date || 'غير متوفر'}\\n\\nالعنوان:\\n${$json.title_raw}\\n\\nالنص:\\n${$json.content_raw}` }] }) }}",
        "options": {
          "timeout": 60000
        }
      },
      "id": "cd12041f-9490-447d-94b2-30c6ae7009fa",
      "name": "Classify with AI",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1120,
        304
      ],
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 2000,
      "credentials": {
        "httpHeaderAuth": {
          "id": "jtSVrGAaoOTVzEWT",
          "name": "Header Auth account"
        }
      },
      "onError": "continueRegularOutput"
    },
    {
      "parameters": {
        "assignments": {
          "assignments": [
            {
              "id": "p1",
              "name": "system_prompt",
              "value": "أنت مصنِّف آلي في منصة رصد قانوني داخلية لإدارة قانونية.\n\nمهمتك محدودة بدقة: تلخيص وتصنيف واستخراج بيانات وصفية من نص صادر عن جهة رسمية.\n\nلا تفعل ما يلي إطلاقاً:\n- لا تخترع أي معلومة غير موجودة في النص.\n- لا تفسّر النظام أو تشرح أثره القانوني.\n- لا تستنتج تاريخاً أو رقماً أو جهة غير مذكورة صراحة.\n- لا تعدّل المعنى القانوني أو تعيد صياغته بما يغيّره.\nإن لم تكن المعلومة في النص، أعد null أو قائمة فارغة. الصمت أصح من التخمين.\n\nقرارك الأول: هل هذا تحديث قانوني أو تنظيمي حقيقي؟\n\nاعتبره تحديثاً قانونياً (is_legal_update = true) إذا كان: نظاماً أو قانوناً جديداً، مرسوماً ملكياً أو سلطانياً أو أميرياً، قراراً وزارياً، لائحة تنفيذية، تعميماً، إطاراً تنظيمياً، إشعاراً رسمياً، متطلب امتثال، موعداً تنظيمياً نهائياً، سابقة قضائية، مشروعاً مطروحاً للاستطلاع، تغييراً ضريبياً أو جمركياً، تنظيماً للعمل أو التراخيص أو حماية البيانات أو الأمن السيبراني أو المنافسة أو التنظيم المالي.\n\nاعتبره غير قانوني (is_legal_update = false) إذا كان: خبر مؤتمر أو اجتماع أو ورشة، مذكرة تفاهم، زيارة رسمية، فعالية، مقابلة، بيان صحفي، إحصاءات، تقرير دوري، مقال رأي، محتوى تسويقي أو ترويجي، تعيينات إدارية، أو خبر قديم يعيد نشر ما سبق.\n\nعند الشك، اخفض قيمة confidence بدل أن ترفع is_legal_update. المنصة تتعامل مع الثقة المنخفضة بالرفض، وهذا هو السلوك المطلوب.\n\nأعد كائن JSON واحداً فقط. لا نص قبله ولا بعده، ولا شرح، ولا علامات markdown.\n\n{\"title_ar\":\"\",\"summary_ar\":\"\",\"country\":\"SA|AE|KW|QA|BH|OM|GCC\",\"category\":\"tax|customs|employment|corporate|financial|capital_markets|banking|data_privacy|cybersecurity|competition|intellectual_property|litigation|licensing|real_estate|energy|healthcare|trade|general\",\"document_type\":\"law|royal_decree|ministerial_decision|executive_regulation|circular|regulatory_framework|official_notice|court_precedent|consultation_draft|other\",\"legal_status\":\"enacted|effective|draft|amended|repealed|pending\",\"is_legal_update\":true,\"confidence\":0.98,\"publication_date\":null,\"effective_date\":null,\"affected_entities\":[],\"keywords\":[]}\n\nالقيم المسموحة للحقول المقيّدة محصورة في القوائم أعلاه حرفياً. أي قيمة خارجها تُرفَض ولا تُصحَّح، فاختر من القائمة أو اختر general / other.",
              "type": "string"
            }
          ]
        },
        "includeOtherFields": true,
        "options": {}
      },
      "id": "b413aaa7-4dc4-46fe-bbe0-d139adb1af3c",
      "name": "Classification prompt",
      "type": "n8n-nodes-base.set",
      "typeVersion": 3.4,
      "position": [
        1024,
        160
      ]
    },
    {
      "parameters": {
        "jsCode": "// Carries the RawItem alongside the AI response so the validator can pair\n// them. n8n keeps item order through the HTTP node, but pairing explicitly is\n// cheaper than relying on that.\nconst items = $('Normalise RawItem').all().map(i => i.json);\nreturn $input.all().map((entry, i) => ({ json: { ...entry.json, __item: items[i] } }));"
      },
      "id": "7dbdba25-3c1c-4145-a98d-a87dc926b6a8",
      "name": "Pair with RawItem",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1328,
        304
      ]
    },
    {
      "parameters": {
        "jsCode": "// Parses and validates the model's JSON. FAIL CLOSED.\n//\n// Anything malformed, incomplete, or outside the allowed enums is rejected\n// outright — never coerced, never defaulted. A coerced category would put a\n// confident wrong classification in a legal archive, which is worse than a gap.\n\nconst COUNTRY = ['SA','AE','KW','QA','BH','OM','GCC'];\nconst CATEGORY = ['tax','customs','employment','corporate','financial','capital_markets','banking','data_privacy','cybersecurity','competition','intellectual_property','litigation','licensing','real_estate','energy','healthcare','trade','general'];\nconst DOC_TYPE = ['law','royal_decree','ministerial_decision','executive_regulation','circular','regulatory_framework','official_notice','court_precedent','consultation_draft','other'];\nconst STATUS = ['enacted','effective','draft','amended','repealed','pending'];\n\nconst ISO_DATE = /^\\d{4}-\\d{2}-\\d{2}$/;\nconst isDate = (v) => {\n  if (v === null || v === undefined || v === '') return true;\n  if (typeof v !== 'string' || !ISO_DATE.test(v)) return false;\n  const d = new Date(v + 'T00:00:00Z');\n  return !Number.isNaN(d.getTime()) && d.toISOString().slice(0,10) === v;\n};\nconst strList = (v) => Array.isArray(v) && v.every(x => typeof x === 'string');\nconst nonEmpty = (v) => typeof v === 'string' && v.trim().length > 0;\n\nconst out = [];\n\nfor (const entry of $input.all()) {\n  const item = entry.json.__item;\n  const response = entry.json;\n\n  const fail = (reason, detail) => out.push({ json: {\n    __item: item, __rejected: true, reason, detail: detail ? String(detail).slice(0,300) : null,\n  }});\n\n  // The Messages API returns content blocks; the text block carries the JSON.\n  const text = response?.content?.[0]?.text;\n  if (!nonEmpty(text)) { fail('ai_empty_response'); continue; }\n\n  let ai;\n  try {\n    ai = JSON.parse(text.trim());\n  } catch {\n    // One tolerated shape: the model wrapped the object in prose or a fence.\n    const m = text.match(/\\{[\\s\\S]*\\}/);\n    if (!m) { fail('ai_parse_failure', text); continue; }\n    try { ai = JSON.parse(m[0]); } catch { fail('ai_parse_failure', text); continue; }\n  }\n\n  if (!ai || typeof ai !== 'object' || Array.isArray(ai)) { fail('ai_not_an_object'); continue; }\n\n  if (!nonEmpty(ai.title_ar)) { fail('ai_invalid_output', 'title_ar'); continue; }\n  if (!nonEmpty(ai.summary_ar)) { fail('ai_invalid_output', 'summary_ar'); continue; }\n  if (!COUNTRY.includes(ai.country)) { fail('ai_invalid_output', 'country=' + ai.country); continue; }\n  if (!CATEGORY.includes(ai.category)) { fail('ai_invalid_output', 'category=' + ai.category); continue; }\n  if (!DOC_TYPE.includes(ai.document_type)) { fail('ai_invalid_output', 'document_type=' + ai.document_type); continue; }\n  if (!STATUS.includes(ai.legal_status)) { fail('ai_invalid_output', 'legal_status=' + ai.legal_status); continue; }\n  if (typeof ai.is_legal_update !== 'boolean') { fail('ai_invalid_output', 'is_legal_update'); continue; }\n  if (typeof ai.confidence !== 'number' || !Number.isFinite(ai.confidence) || ai.confidence < 0 || ai.confidence > 1) {\n    fail('ai_invalid_output', 'confidence=' + ai.confidence); continue;\n  }\n  if (!isDate(ai.effective_date)) { fail('ai_invalid_output', 'effective_date=' + ai.effective_date); continue; }\n  if (!isDate(ai.publication_date)) { fail('ai_invalid_output', 'publication_date=' + ai.publication_date); continue; }\n  if (ai.affected_entities !== undefined && !strList(ai.affected_entities)) { fail('ai_invalid_output', 'affected_entities'); continue; }\n  if (ai.keywords !== undefined && !strList(ai.keywords)) { fail('ai_invalid_output', 'keywords'); continue; }\n\n  out.push({ json: {\n    __item: item,\n    __rejected: false,\n    ai: {\n      title_ar: ai.title_ar.trim(),\n      summary_ar: ai.summary_ar.trim(),\n      country: ai.country,\n      category: ai.category,\n      document_type: ai.document_type,\n      legal_status: ai.legal_status,\n      is_legal_update: ai.is_legal_update,\n      confidence: ai.confidence,\n      publication_date: ai.publication_date || null,\n      effective_date: ai.effective_date || null,\n      affected_entities: (ai.affected_entities || []).slice(0, 20),\n      keywords: (ai.keywords || []).slice(0, 20),\n    },\n  }});\n}\n\nreturn out;"
      },
      "id": "081721b7-6539-42f3-a272-71bf53516dc5",
      "name": "Validate AI output",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1520,
        304
      ]
    },
    {
      "parameters": {
        "jsCode": "// Builds the string that becomes content_hash, and drops items the AI stage\n// already rejected.\n//\n// Hash inputs: source_id | source_url | title | publication_date. The URL is\n// included deliberately — two authorities republishing the same instrument at\n// different URLs are two archive entries, and the M3 exclusion-group rule\n// handles the mirror case instead.\n\nconst out = [];\nfor (const entry of $input.all()) {\n  const e = entry.json;\n  if (e.__rejected) { out.push({ json: e }); continue; }\n\n  const item = e.__item;\n  const pubDate = item.publication_date || e.ai.publication_date || '';\n\n  out.push({ json: {\n    ...e,\n    hash_input: [item.source_id, item.source_url, e.ai.title_ar, pubDate].join('|'),\n  }});\n}\nreturn out;"
      },
      "id": "5a4585a1-fddb-40a4-a367-c0e4cfa3b01d",
      "name": "Build hash input",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1728,
        304
      ]
    },
    {
      "parameters": {
        "type": "SHA256",
        "value": "={{ $json.hash_input || 'skipped' }}",
        "dataPropertyName": "content_hash",
        "encoding": "hex"
      },
      "id": "0cfdb2cb-f293-4288-bba3-6fb6a2501b11",
      "name": "SHA256 content hash",
      "type": "n8n-nodes-base.crypto",
      "typeVersion": 1,
      "position": [
        1920,
        304
      ]
    },
    {
      "parameters": {
        "jsCode": "// Prepares the Publishing Gate payload and reads the fail-closed threshold.\n//\n// ai.confidence_threshold is deliberately NOT defaulted here. If it is missing\n// or malformed the gate rejects everything — defaulting it could quietly widen\n// what publishes, so absence must be loud.\n\nconst settings = $('Get threshold').all().map(i => i.json);\nconst row = settings.find(r => r.key === 'ai.confidence_threshold');\nconst threshold = row && typeof row.value === 'number' ? row.value : null;\n\nconst out = [];\nfor (const entry of $input.all()) {\n  const e = entry.json;\n  if (e.__rejected) {\n    // Rejected during AI-response validation: still needs source_id/content_hash\n    // in the same `item` shape the gate expects, or the gate's source re-read\n    // and hash lookup receive \"undefined\" and the Supabase call throws instead\n    // of a clean rejection. The gate short-circuits on __rejected and never\n    // reads `ai`, so it is left null rather than reconstructed.\n    out.push({ json: {\n      __rejected: true,\n      reason: e.reason,\n      detail: e.detail,\n      confidence_threshold: threshold,\n      ai: null,\n      item: {\n        source_id: e.__item.source_id,\n        source_url: e.__item.source_url,\n        content_hash: e.content_hash,\n        publication_date: e.__item.publication_date,\n        raw_excerpt: (e.__item.content_raw || e.__item.title_raw || '').slice(0, 20000),\n        document_path: (e.__item.attachments && e.__item.attachments[0]) || null,\n        ai_model: 'claude-sonnet-5',\n      },\n    }});\n    continue;\n  }\n\n  const item = e.__item;\n  out.push({ json: {\n    __rejected: false,\n    confidence_threshold: threshold,\n    ai: e.ai,\n    item: {\n      source_id: item.source_id,\n      source_url: item.source_url,\n      content_hash: e.content_hash,\n      publication_date: item.publication_date,\n      // Verbatim source text, stored so a lawyer can check the summary against\n      // what the authority actually published.\n      raw_excerpt: (item.content_raw || item.title_raw || '').slice(0, 20000),\n      // The official document URL, preserved end to end.\n      document_path: (item.attachments && item.attachments[0]) || null,\n      ai_model: 'claude-sonnet-5',\n    },\n  }});\n}\nreturn out;"
      },
      "id": "66a59970-b03f-4d7e-8c52-a3367cd7aaf4",
      "name": "Prepare gate payload",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2128,
        304
      ]
    },
    {
      "parameters": {
        "workflowId": {
          "__rl": true,
          "value": "2AmFW6QzgNwSxaOU",
          "mode": "list",
          "cachedResultUrl": "/workflow/2AmFW6QzgNwSxaOU",
          "cachedResultName": "03 — Publishing Gate"
        },
        "workflowInputs": {
          "mappingMode": "defineBelow",
          "value": {},
          "matchingColumns": [],
          "schema": [],
          "attemptToConvertTypes": false,
          "convertFieldsToString": true
        },
        "options": {
          "waitForSubWorkflow": true
        },
        "mode": "each"
      },
      "id": "249c86c5-e456-4706-a3b2-31c36ffae3ce",
      "name": "Publishing Gate",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1.2,
      "position": [
        2320,
        304
      ]
    },
    {
      "parameters": {
        "jsCode": "// Aggregates every outcome for this source into ONE workflow_logs row.\n//\n// One row per source per run, not one per item: workflow_logs records\n// executions, and the dashboard aggregates from it. Rejections are counted in\n// rejection_reasons rather than logged individually — a rejection is a normal\n// decision, not an incident.\n\nconst src = $('Called by scheduler').first().json.source;\nconst started = $('Called by scheduler').first().json.dispatched_at || new Date().toISOString();\nconst trigger = $('Called by scheduler').first().json.trigger_type || 'scheduled';\nconst correlationId = $('Called by scheduler').first().json.correlation_id || null;\n\nlet fetched = 0, published = 0, rejected = 0;\nconst reasons = {};\nlet errorMessage = null;\n\nfor (const entry of $input.all()) {\n  const e = entry.json;\n  fetched += 1;\n\n  if (e.__rejected) {\n    rejected += 1;\n    reasons[e.reason] = (reasons[e.reason] || 0) + 1;\n    continue;\n  }\n  if (e.outcome === 'published') { published += 1; continue; }\n  if (e.outcome === 'duplicate') { rejected += 1; reasons.duplicate = (reasons.duplicate || 0) + 1; continue; }\n  if (e.outcome === 'rejected') {\n    rejected += 1;\n    reasons[e.reason || 'unknown'] = (reasons[e.reason || 'unknown'] || 0) + 1;\n    continue;\n  }\n  if (e.outcome === 'insert_failed') {\n    rejected += 1;\n    reasons.insert_failed = (reasons.insert_failed || 0) + 1;\n    errorMessage = e.error_message || errorMessage;\n  }\n}\n\nconst finished = new Date().toISOString();\n\n// partial: some items failed to insert. failed is reserved for a run that\n// could not fetch at all, which the M10 failure branch records.\nconst status = errorMessage ? 'partial' : 'success';\n\nreturn [{ json: {\n  workflow_name: '02-source-ingestion',\n  execution_id: String($execution.id),\n  source_id: src.id,\n  trigger_type: trigger,\n  correlation_id: correlationId,\n  status,\n  items_fetched: fetched,\n  items_published: published,\n  items_rejected: rejected,\n  rejection_reasons: reasons,\n  error_message: errorMessage,\n  duration_ms: Math.max(0, new Date(finished).getTime() - new Date(started).getTime()),\n  retry_attempt: 0,\n  started_at: started,\n  finished_at: finished,\n}}];"
      },
      "id": "93bf6454-c769-483b-96b7-6c96535ea99a",
      "name": "Summarise run",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2528,
        304
      ]
    },
    {
      "parameters": {
        "tableId": "workflow_logs",
        "dataToSend": "autoMapInputData"
      },
      "id": "170a7b3f-ac3f-48af-8f31-93784e1be5bd",
      "name": "Write workflow log",
      "type": "n8n-nodes-base.supabase",
      "typeVersion": 1,
      "position": [
        2720,
        304
      ],
      "credentials": {
        "supabaseApi": {
          "id": "10zDIxzQYFnJuOmh",
          "name": "Supabase account 2"
        }
      }
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "version": 2
          },
          "conditions": [
            {
              "leftValue": "={{ $json.__empty }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "equals"
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "14acb990-8ec7-4e61-b3f4-76f9560f55a9",
      "name": "Has items?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [
        1024,
        304
      ]
    },
    {
      "parameters": {
        "jsCode": "// Deliberate empty-run outcome: the source fetched cleanly but produced\n// nothing normalisable (or everything was filtered by domain/shape). This\n// is a successful run, logged as such, not silently dropped.\nconst src = $('Called by scheduler').first().json.source;\nconst started = $('Called by scheduler').first().json.dispatched_at || new Date().toISOString();\nconst trigger = $('Called by scheduler').first().json.trigger_type || 'scheduled';\nconst correlationId = $('Called by scheduler').first().json.correlation_id || null;\nconst marker = $input.first().json;\nconst finished = new Date().toISOString();\n\nreturn [{ json: {\n  workflow_name: '02-source-ingestion',\n  execution_id: String($execution.id),\n  source_id: src.id,\n  trigger_type: trigger,\n  correlation_id: correlationId,\n  status: 'empty',\n  items_fetched: marker.raw_count || 0,\n  items_published: 0,\n  items_rejected: 0,\n  rejection_reasons: marker.rejected || {},\n  error_message: null,\n  duration_ms: Math.max(0, new Date(finished).getTime() - new Date(started).getTime()),\n  retry_attempt: 0,\n  started_at: started,\n  finished_at: finished,\n}}];\n"
      },
      "id": "74754020-9bb6-4c14-a471-cd1de0ba61cf",
      "name": "Build empty run summary",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1200,
        460
      ]
    },
    {
      "parameters": {
        "jsCode": "// A source that could not be fetched at all still needs a workflow_logs\n// row (status='failed') and must not leave the webhook response hanging.\nconst started = $('Called by scheduler').first().json.dispatched_at || new Date().toISOString();\nconst trigger = $('Called by scheduler').first().json.trigger_type || 'scheduled';\nconst correlationId = $('Called by scheduler').first().json.correlation_id || null;\nconst e = $input.first().json;\nconst finished = new Date().toISOString();\n\nreturn [{ json: {\n  workflow_name: '02-source-ingestion',\n  execution_id: String($execution.id),\n  source_id: e.source_id,\n  trigger_type: trigger,\n  correlation_id: correlationId,\n  status: 'failed',\n  items_fetched: 0,\n  items_published: 0,\n  items_rejected: 0,\n  rejection_reasons: {},\n  error_message: e.error_message || 'fetch failed',\n  duration_ms: Math.max(0, new Date(finished).getTime() - new Date(started).getTime()),\n  retry_attempt: 0,\n  started_at: started,\n  finished_at: finished,\n}}];\n"
      },
      "id": "05905a1a-1ceb-4fe7-9a0f-c12dd89b980f",
      "name": "Build failed run summary",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        640,
        800
      ]
    },
    {
      "parameters": {
        "jsCode": "// Re-emit the normalised items now that the threshold read (previous\n// node) is guaranteed to have executed. Get threshold has no data\n// dependency on these items -- it only needs to run before 'Prepare gate\n// payload' references it via $('Get threshold'), and a node with no\n// outgoing connection is not guaranteed to run by n8n's execution planner.\nreturn $('Normalise RawItem').all();\n"
      },
      "id": "e5de34d7-1517-43d2-9057-cd27ec9d95fc",
      "name": "Continue after threshold",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1200,
        160
      ]
    }
  ],
  "connections": {
    "Called by scheduler": {
      "main": [
        [
          {
            "node": "Parser router",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Parser router": {
      "main": [
        [
          {
            "node": "Fetch RSS",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Fetch API",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Fetch HTML",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Fetch PDF index",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fetch RSS": {
      "main": [
        [
          {
            "node": "Parse XML",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Record fetch failure",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Parse XML": {
      "main": [
        [
          {
            "node": "RSS → RawItem",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "RSS → RawItem": {
      "main": [
        [
          {
            "node": "Merge lanes",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fetch API": {
      "main": [
        [
          {
            "node": "API → RawItem",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Record fetch failure",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "API → RawItem": {
      "main": [
        [
          {
            "node": "Merge lanes",
            "type": "main",
            "index": 1
          }
        ]
      ]
    },
    "Fetch HTML": {
      "main": [
        [
          {
            "node": "Extract listing",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Record fetch failure",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Extract listing": {
      "main": [
        [
          {
            "node": "HTML → RawItem",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "HTML → RawItem": {
      "main": [
        [
          {
            "node": "Merge lanes",
            "type": "main",
            "index": 2
          }
        ]
      ]
    },
    "Fetch PDF index": {
      "main": [
        [
          {
            "node": "PDF index → RawItem",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Record fetch failure",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "PDF index → RawItem": {
      "main": [
        [
          {
            "node": "Merge lanes",
            "type": "main",
            "index": 3
          }
        ]
      ]
    },
    "Merge lanes": {
      "main": [
        [
          {
            "node": "Normalise RawItem",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalise RawItem": {
      "main": [
        [
          {
            "node": "Has items?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Classification prompt": {
      "main": [
        [
          {
            "node": "Classify with AI",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Classify with AI": {
      "main": [
        [
          {
            "node": "Pair with RawItem",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Pair with RawItem": {
      "main": [
        [
          {
            "node": "Validate AI output",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Validate AI output": {
      "main": [
        [
          {
            "node": "Build hash input",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build hash input": {
      "main": [
        [
          {
            "node": "SHA256 content hash",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "SHA256 content hash": {
      "main": [
        [
          {
            "node": "Prepare gate payload",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Prepare gate payload": {
      "main": [
        [
          {
            "node": "Publishing Gate",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Publishing Gate": {
      "main": [
        [
          {
            "node": "Summarise run",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Summarise run": {
      "main": [
        [
          {
            "node": "Write workflow log",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Has items?": {
      "main": [
        [
          {
            "node": "Build empty run summary",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Get threshold",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build empty run summary": {
      "main": [
        [
          {
            "node": "Write workflow log",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Record fetch failure": {
      "main": [
        [
          {
            "node": "Build failed run summary",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build failed run summary": {
      "main": [
        [
          {
            "node": "Write workflow log",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get threshold": {
      "main": [
        [
          {
            "node": "Continue after threshold",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Continue after threshold": {
      "main": [
        [
          {
            "node": "Classification prompt",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "binaryMode": "separate",
    "availableInMCP": false,
    "callerPolicy": "workflowsFromSameOwner"
  },
  "staticData": null,
  "meta": {
    "templateCredsSetupCompleted": true
  },
  "pinData": {},
  "versionId": "91b93426-c238-453d-9df3-034c466248ac",
  "activeVersionId": "91b93426-c238-453d-9df3-034c466248ac",
  "versionCounter": 33,
  "triggerCount": 0,
  "shared": [
    {
      "updatedAt": "2026-08-01T16:46:39.922Z",
      "createdAt": "2026-08-01T16:46:39.922Z",
      "role": "workflow:owner",
      "workflowId": "FHt8uKbBcixIWbWO",
      "projectId": "Fo19xH6yJI9Yypvs",
      "project": {
        "updatedAt": "2026-06-03T20:11:11.632Z",
        "createdAt": "2026-06-03T19:16:50.624Z",
        "id": "Fo19xH6yJI9Yypvs",
        "name": "Asayel Qaid <asayelmq@gmail.com>",
        "type": "personal",
        "icon": null,
        "description": null,
        "creatorId": "c6441430-2935-43cb-a7bd-db0da8ae18ce",
        "projectRelations": [
          {
            "updatedAt": "2026-06-03T19:16:50.624Z",
            "createdAt": "2026-06-03T19:16:50.624Z",
            "userId": "c6441430-2935-43cb-a7bd-db0da8ae18ce",
            "projectId": "Fo19xH6yJI9Yypvs",
            "user": {
              "updatedAt": "2026-08-02T05:54:54.138Z",
              "createdAt": "2026-06-03T19:16:50.624Z",
              "id": "c6441430-2935-43cb-a7bd-db0da8ae18ce",
              "email": "asayelmq@gmail.com",
              "firstName": "Asayel",
              "lastName": "Qaid",
              "personalizationAnswers": {
                "version": "v4",
                "personalization_survey_submitted_at": "2026-06-03T20:12:25.559Z",
                "personalization_survey_n8n_version": "2.7.4",
                "automationGoalDevops": [
                  "reporting",
                  "ticketing-systems-integrations"
                ],
                "companySize": "20-99",
                "companyType": "systems-integrator",
                "role": "it",
                "reportedSource": "friend"
              },
              "settings": {
                "easyAIWorkflowOnboarded": true,
                "firstSuccessfulWorkflowId": "FHt8uKbBcixIWbWO",
                "userActivated": true,
                "userActivatedAt": 1785650091040
              },
              "disabled": false,
              "mfaEnabled": false,
              "lastActiveAt": "2026-08-01",
              "isPending": false
            }
          }
        ]
      }
    }
  ],
  "tags": [
    {
      "updatedAt": "2026-08-01T15:13:31.517Z",
      "createdAt": "2026-08-01T15:13:31.517Z",
      "id": "CFx6g4HIqhyJJh7g",
      "name": "legal-intelligence"
    }
  ],
  "activeVersion": {
    "updatedAt": "2026-08-02T12:25:02.735Z",
    "createdAt": "2026-08-02T12:25:02.735Z",
    "versionId": "91b93426-c238-453d-9df3-034c466248ac",
    "workflowId": "FHt8uKbBcixIWbWO",
    "nodes": [
      {
        "parameters": {},
        "id": "9858366b-8ef5-4bd8-b4a4-107e49c5436f",
        "name": "Called by scheduler",
        "type": "n8n-nodes-base.executeWorkflowTrigger",
        "typeVersion": 1,
        "position": [
          -480,
          304
        ]
      },
      {
        "parameters": {
          "rules": {
            "values": [
              {
                "conditions": {
                  "options": {
                    "caseSensitive": true,
                    "version": 2
                  },
                  "conditions": [
                    {
                      "leftValue": "={{ $json.source.parser_type }}",
                      "rightValue": "rss",
                      "operator": {
                        "type": "string",
                        "operation": "equals"
                      }
                    }
                  ],
                  "combinator": "and"
                }
              },
              {
                "conditions": {
                  "options": {
                    "caseSensitive": true,
                    "version": 2
                  },
                  "conditions": [
                    {
                      "leftValue": "={{ $json.source.parser_type }}",
                      "rightValue": "api",
                      "operator": {
                        "type": "string",
                        "operation": "equals"
                      }
                    }
                  ],
                  "combinator": "and"
                }
              },
              {
                "conditions": {
                  "options": {
                    "caseSensitive": true,
                    "version": 2
                  },
                  "conditions": [
                    {
                      "leftValue": "={{ $json.source.parser_type }}",
                      "rightValue": "html",
                      "operator": {
                        "type": "string",
                        "operation": "equals"
                      }
                    }
                  ],
                  "combinator": "and"
                }
              },
              {
                "conditions": {
                  "options": {
                    "caseSensitive": true,
                    "version": 2
                  },
                  "conditions": [
                    {
                      "leftValue": "={{ $json.source.parser_type }}",
                      "rightValue": "pdf",
                      "operator": {
                        "type": "string",
                        "operation": "equals"
                      }
                    }
                  ],
                  "combinator": "and"
                }
              }
            ]
          },
          "options": {
            "fallbackOutput": "none"
          }
        },
        "id": "53c083b9-d01b-41f7-a19a-9d0664cd5359",
        "name": "Parser router",
        "type": "n8n-nodes-base.switch",
        "typeVersion": 3.2,
        "position": [
          -256,
          304
        ]
      },
      {
        "parameters": {
          "url": "={{ $json.source.feed_url }}",
          "options": {
            "response": {
              "response": {
                "responseFormat": "text"
              }
            },
            "timeout": 30000
          }
        },
        "id": "79792d0e-bcaf-4ab9-9147-08f3ef970407",
        "name": "Fetch RSS",
        "type": "n8n-nodes-base.httpRequest",
        "typeVersion": 4.2,
        "position": [
          0,
          0
        ],
        "retryOnFail": true,
        "maxTries": 3,
        "waitBetweenTries": 2000,
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "options": {}
        },
        "id": "421b3d1c-9b4b-4f2e-8de4-863036d46c30",
        "name": "Parse XML",
        "type": "n8n-nodes-base.xml",
        "typeVersion": 1,
        "position": [
          224,
          0
        ]
      },
      {
        "parameters": {
          "jsCode": "// RSS lane → RawItem[]\n//\n// parser_config may name which fields carry the date and body; both default\n// to the RSS conventions. Nothing here is guessed at crawl time — an\n// unconfigured source never reaches this workflow, because it cannot be\n// activated.\n\nconst src = $('Called by scheduler').first().json.source;\nconst cfg = src.parser_config || {};\nconst dateField = cfg.date_field || 'pubDate';\nconst contentField = cfg.content_field || 'description';\n\nconst root = $input.first().json;\nconst channel = root?.rss?.channel ?? root?.feed ?? {};\nlet entries = channel.item ?? channel.entry ?? root?.feed?.entry ?? [];\nif (!Array.isArray(entries)) entries = [entries];\n\nconst text = (v) => {\n  if (v === null || v === undefined) return '';\n  if (typeof v === 'string') return v;\n  if (typeof v === 'object') return v._ ?? v['#text'] ?? '';\n  return String(v);\n};\n\nconst toIsoDate = (v) => {\n  const raw = text(v);\n  if (!raw) return null;\n  const d = new Date(raw);\n  // An unparseable date becomes null. The AI is instructed never to invent\n  // one, and neither does this.\n  return Number.isNaN(d.getTime()) ? null : d.toISOString().slice(0, 10);\n};\n\nconst linkOf = (e) => {\n  if (typeof e.link === 'string') return e.link;\n  if (Array.isArray(e.link)) return e.link[0]?.$?.href ?? text(e.link[0]);\n  return e.link?.$?.href ?? text(e.link) ?? e.guid ? text(e.guid) : '';\n};\n\nreturn entries.filter(Boolean).map((e) => ({\n  json: {\n    source_id: src.id,\n    source_url: linkOf(e),\n    title_raw: text(e.title),\n    content_raw: text(e[contentField]) || text(e.summary) || text(e.content),\n    publication_date: toIsoDate(e[dateField] ?? e.published ?? e.updated),\n    attachments: [],\n    fetched_at: new Date().toISOString(),\n  },\n}));"
        },
        "id": "40ab11fd-8caa-4050-a276-25af3ffbdbf9",
        "name": "RSS → RawItem",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          448,
          0
        ]
      },
      {
        "parameters": {
          "url": "={{ $json.source.feed_url }}",
          "sendHeaders": true,
          "specifyHeaders": "json",
          "jsonHeaders": "={{ JSON.stringify($json.source.parser_config.headers || {}) }}",
          "options": {
            "timeout": 30000
          }
        },
        "id": "3d0cd50a-feef-41f2-a744-20d39b4cb093",
        "name": "Fetch API",
        "type": "n8n-nodes-base.httpRequest",
        "typeVersion": 4.2,
        "position": [
          0,
          208
        ],
        "retryOnFail": true,
        "maxTries": 3,
        "waitBetweenTries": 2000,
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "jsCode": "// API lane → RawItem[]\n// Field paths come from parser_config, filled in by an admin who inspected\n// the real response. Nothing is inferred.\n\nconst src = $('Called by scheduler').first().json.source;\nconst cfg = src.parser_config || {};\n\nconst dig = (obj, path) =>\n  (path || '').split('.').filter(Boolean).reduce((o, k) => (o == null ? undefined : o[k]), obj);\n\nconst payload = $input.first().json;\nlet items = cfg.items_path ? dig(payload, cfg.items_path) : payload;\nif (!Array.isArray(items)) items = items ? [items] : [];\n\nconst toIsoDate = (raw) => {\n  if (!raw) return null;\n  const d = new Date(raw);\n  return Number.isNaN(d.getTime()) ? null : d.toISOString().slice(0, 10);\n};\n\nreturn items.map((it) => ({\n  json: {\n    source_id: src.id,\n    source_url: String(dig(it, cfg.url) ?? ''),\n    title_raw: String(dig(it, cfg.title) ?? ''),\n    content_raw: String(dig(it, cfg.body) ?? ''),\n    publication_date: toIsoDate(dig(it, cfg.date)),\n    attachments: [],\n    fetched_at: new Date().toISOString(),\n  },\n}));"
        },
        "id": "cc84dd24-e6a7-4725-b99d-4d75a19900ed",
        "name": "API → RawItem",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          224,
          208
        ]
      },
      {
        "parameters": {
          "url": "={{ $json.source.base_url }}",
          "options": {
            "response": {
              "response": {
                "responseFormat": "text"
              }
            },
            "timeout": 30000
          }
        },
        "id": "537b9077-bd28-4f2c-8978-ba5ada3f26ba",
        "name": "Fetch HTML",
        "type": "n8n-nodes-base.httpRequest",
        "typeVersion": 4.2,
        "position": [
          0,
          400
        ],
        "retryOnFail": true,
        "maxTries": 3,
        "waitBetweenTries": 2000,
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "operation": "extractHtmlContent",
          "extractionValues": {
            "values": [
              {
                "key": "items",
                "cssSelector": "={{ $('Called by scheduler').first().json.source.parser_config.list }}",
                "returnValue": "html",
                "returnArray": true
              }
            ]
          },
          "options": {}
        },
        "id": "be3851f6-129e-4606-9181-99e54393d774",
        "name": "Extract listing",
        "type": "n8n-nodes-base.html",
        "typeVersion": 1.2,
        "position": [
          224,
          400
        ]
      },
      {
        "parameters": {
          "jsCode": "// HTML lane → RawItem[]\n//\n// Selectors come from parser_config, which an admin filled in against the\n// live page during verification. A source with an empty config cannot be\n// marked verified and therefore cannot be active, so this never runs on\n// guessed selectors.\n\nconst src = $('Called by scheduler').first().json.source;\nconst cfg = src.parser_config || {};\n\n// Extract origin without using the URL class,\n// because this n8n environment does not expose it inside Code nodes.\nconst baseUrl = String(src.base_url || '').replace(/\\/+$/, '');\nconst originMatch = baseUrl.match(/^(https?:\\/\\/[^/]+)/i);\nconst origin = originMatch ? originMatch[1] : baseUrl;\n\nconst blocks = $input.first().json.items || [];\n\n// Minimal extraction over the fragment HTML. Kept dependency-free: the\n// HTML node has already narrowed the document to the list items.\nconst pick = (html, selector) => {\n  if (!selector) return '';\n  const attrMatch = selector.match(/^(.*)@([\\w-]+)$/);\n  const sel = (attrMatch ? attrMatch[1] : selector).trim();\n  const attr = attrMatch ? attrMatch[2] : null;\n  const tag = sel.split(/[\\s>]/).pop().replace(/[.#].*$/, '') || 'a';\n  const re = new RegExp(`<${tag}[^>]*>([\\\\s\\\\S]*?)</${tag}>`, 'i');\n  if (attr) {\n    const attrRe = new RegExp(`<${tag}[^>]*\\\\s${attr}=[\"']([^\"']+)[\"']`, 'i');\n    const m = html.match(attrRe);\n    return m ? m[1] : '';\n  }\n  const m = html.match(re);\n  return m ? m[1].replace(/<[^>]+>/g, ' ').replace(/\\s+/g, ' ').trim() : '';\n};\n\nconst absolute = (href) => {\n  const value = String(href || '').trim();\n  if (!value) return '';\n  if (/^https?:\\/\\//i.test(value)) return value;\n  if (value.startsWith('//')) return `https:${value}`;\n  if (value.startsWith('/')) return `${origin}${value}`;\n  return `${baseUrl}/${value.replace(/^\\/+/, '')}`;\n};\n\nconst toIsoDate = (raw) => {\n  if (!raw) return null;\n  const d = new Date(raw);\n  return Number.isNaN(d.getTime()) ? null : d.toISOString().slice(0, 10);\n};\n\nreturn blocks.map((html) => {\n  const link = absolute(pick(html, cfg.link));\n  const pdfMatch = html.match(/href=[\"']([^\"']+\\.pdf)[\"']/i);\n  return {\n    json: {\n      source_id: src.id,\n      source_url: link,\n      title_raw: pick(html, cfg.title),\n      content_raw: pick(html, cfg.body),\n      publication_date: toIsoDate(pick(html, cfg.date)),\n      // PDF enrichment applies to every lane, not just parser_type = pdf:\n      // gazettes routinely publish a listing entry whose substance is an\n      // attached document.\n      attachments: pdfMatch ? [absolute(pdfMatch[1])] : [],\n      fetched_at: new Date().toISOString(),\n    },\n  };\n});\n"
        },
        "id": "66a54f22-2502-497c-94b1-ff5c01c7a351",
        "name": "HTML → RawItem",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          448,
          400
        ]
      },
      {
        "parameters": {
          "url": "={{ $json.source.base_url }}",
          "options": {
            "response": {
              "response": {
                "responseFormat": "text"
              }
            },
            "timeout": 30000
          }
        },
        "id": "e6cbeaaa-36b2-4d32-b352-ee4ce1adab5f",
        "name": "Fetch PDF index",
        "type": "n8n-nodes-base.httpRequest",
        "typeVersion": 4.2,
        "position": [
          0,
          608
        ],
        "retryOnFail": true,
        "maxTries": 3,
        "waitBetweenTries": 2000,
        "onError": "continueErrorOutput"
      },
      {
        "parameters": {
          "jsCode": "const src = $('Called by scheduler').first().json.source;\nconst cfg = src.parser_config || {};\nconst maxPages = Number(cfg.max_pages) || 40;\n\nconst baseUrl = String(src.base_url || '').replace(/\\/+$/, '');\nconst originMatch = baseUrl.match(/^(https?:\\/\\/[^/]+)/i);\nconst origin = originMatch ? originMatch[1] : baseUrl;\n\nconst makeAbsolute = (href) => {\n  const value = String(href || '').trim();\n\n  if (!value) return '';\n  if (/^https?:\\/\\//i.test(value)) return value;\n  if (value.startsWith('//')) return `https:${value}`;\n  if (value.startsWith('/')) return `${origin}${value}`;\n\n  return `${baseUrl}/${value.replace(/^\\/+/, '')}`;\n};\n\nconst input = $input.first().json;\n\nconst html =\n  typeof input.data === 'string'\n    ? input.data\n    : typeof input.body === 'string'\n      ? input.body\n      : '';\n\nconst links = [...html.matchAll(\n  /href=[\"']([^\"']+\\.pdf(?:\\?[^\"']*)?)[\"'][^>]*>([\\s\\S]*?)</gi\n)]\n  .slice(0, maxPages)\n  .map((match) => ({\n    href: makeAbsolute(match[1]),\n    label: match[2]\n      .replace(/<[^>]+>/g, ' ')\n      .replace(/\\s+/g, ' ')\n      .trim(),\n  }))\n  .filter((link) => link.href);\n\nreturn links.map((link) => ({\n  json: {\n    source_id: src.id,\n    source_url: link.href,\n    title_raw: link.label || 'عدد من الجريدة الرسمية أم القرى',\n    content_raw: '',\n    publication_date: null,\n    attachments: [link.href],\n    fetched_at: new Date().toISOString(),\n  },\n}));"
        },
        "id": "7c4ba6b3-537b-4aff-8c0a-c728f7b1e896",
        "name": "PDF index → RawItem",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          224,
          608
        ]
      },
      {
        "parameters": {
          "numberInputs": 4
        },
        "id": "436ee566-779a-4d1e-89f6-90da3c920321",
        "name": "Merge lanes",
        "type": "n8n-nodes-base.merge",
        "typeVersion": 3,
        "position": [
          688,
          304
        ]
      },
      {
        "parameters": {
          "jsCode": "// Normalises and validates every RawItem before anything downstream sees it.\n//\n// This is the single contract between parsing and the M9 AI stage: whatever\n// lane produced an item, it leaves here in the same shape or not at all.\n\nconst src = $('Called by scheduler').first().json.source;\n\nconst allowed = (src.allowed_domains || [])\n  .map((domain) => String(domain).trim().toLowerCase())\n  .filter(Boolean);\n\nconst out = [];\nconst rejected = {\n  no_url: 0,\n  no_title: 0,\n  domain_mismatch: 0,\n};\n\nfor (const item of $input.all()) {\n  const r = item.json;\n\n  const sourceUrl = String(r.source_url || '').trim();\n  const titleRaw = String(r.title_raw || '').trim();\n\n  if (!sourceUrl) {\n    rejected.no_url += 1;\n    continue;\n  }\n\n  if (!titleRaw) {\n    rejected.no_title += 1;\n    continue;\n  }\n\n  // Extract hostname without using the URL class,\n  // because this n8n environment does not expose it inside Code nodes.\n  const hostMatch = sourceUrl.match(/^https?:\\/\\/([^/:?#]+)/i);\n\n  if (!hostMatch) {\n    rejected.no_url += 1;\n    continue;\n  }\n\n  const host = hostMatch[1].toLowerCase();\n\n  if (!allowed.includes(host)) {\n    rejected.domain_mismatch += 1;\n    continue;\n  }\n\n  out.push({\n    json: {\n      source_id: src.id,\n      source_url: sourceUrl,\n      title_raw: titleRaw.slice(0, 2000),\n      content_raw: String(r.content_raw || '').trim().slice(0, 50000),\n      publication_date: r.publication_date || null,\n      attachments: Array.isArray(r.attachments)\n        ? r.attachments.slice(0, 5)\n        : [],\n      fetched_at: r.fetched_at || new Date().toISOString(),\n\n      // Carried through so M9 can log against the correct source.\n      source: {\n        id: src.id,\n        country: src.country,\n        authority_ar: src.authority_ar,\n        allowed_domains: src.allowed_domains,\n      },\n    },\n  });\n}\n\nconsole.log(\n  `normalised ${out.length} items; rejected ${JSON.stringify(rejected)}`\n);\n\n// A genuinely empty result must still leave exactly one item behind, or every\n// downstream node (including the log write) is skipped by n8n's zero-item rule\n// and the run vanishes without a workflow_logs row or a webhook response.\nif (out.length === 0) {\n  return [{ json: { __empty: true, raw_count: $input.all().length, rejected } }];\n}\n\n// M9 attaches the AI stage and the Publishing Gate to this output.\nreturn out;"
        },
        "id": "871b4a68-729a-4d68-9886-b6e57016a7e3",
        "name": "Normalise RawItem",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          912,
          304
        ]
      },
      {
        "parameters": {
          "jsCode": "// Fetch failure for this source.\n//\n// Isolated on purpose: one unreachable source must not stop the others in the\n// same scheduler tick. M10 turns this into retry state and a health snapshot;\n// for now it records the failure and ends this source's run.\n\nconst src = $('Called by scheduler').first().json.source;\nconst err = $input.first().json.error || $input.first().json;\n\nreturn [{\n  json: {\n    failed: true,\n    source_id: src.id,\n    authority_ar: src.authority_ar,\n    error_message: String(err?.message || err?.reason || 'fetch failed').slice(0, 1000),\n  },\n}];"
        },
        "id": "785ba1b9-0edb-476e-bb8f-988f21151f97",
        "name": "Record fetch failure",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          448,
          800
        ]
      },
      {
        "parameters": {
          "operation": "getAll",
          "tableId": "app_settings",
          "returnAll": true
        },
        "id": "db447710-99b0-46f5-ad12-04dcaadf2010",
        "name": "Get threshold",
        "type": "n8n-nodes-base.supabase",
        "typeVersion": 1,
        "position": [
          912,
          528
        ],
        "alwaysOutputData": true,
        "credentials": {
          "supabaseApi": {
            "id": "10zDIxzQYFnJuOmh",
            "name": "Supabase account 2"
          }
        }
      },
      {
        "parameters": {
          "method": "POST",
          "url": "https://api.anthropic.com/v1/messages",
          "authentication": "genericCredentialType",
          "genericAuthType": "httpHeaderAuth",
          "sendHeaders": true,
          "specifyHeaders": "json",
          "jsonHeaders": "={{ JSON.stringify({ \"anthropic-version\": \"2023-06-01\", \"content-type\": \"application/json\" }) }}",
          "sendBody": true,
          "specifyBody": "json",
          "jsonBody": "={{ JSON.stringify({ model: 'claude-sonnet-5', max_tokens: 1500, temperature: 0, system: $('Classification prompt').first().json.system_prompt, messages: [{ role: 'user', content: `الجهة: ${$json.source.authority_ar}\\nالدولة المسجّلة للمصدر: ${$json.source.country}\\nالرابط: ${$json.source_url}\\nتاريخ النشر المستخرج من المصدر: ${$json.publication_date || 'غير متوفر'}\\n\\nالعنوان:\\n${$json.title_raw}\\n\\nالنص:\\n${$json.content_raw}` }] }) }}",
          "options": {
            "timeout": 60000
          }
        },
        "id": "cd12041f-9490-447d-94b2-30c6ae7009fa",
        "name": "Classify with AI",
        "type": "n8n-nodes-base.httpRequest",
        "typeVersion": 4.2,
        "position": [
          1120,
          304
        ],
        "retryOnFail": true,
        "maxTries": 3,
        "waitBetweenTries": 2000,
        "credentials": {
          "httpHeaderAuth": {
            "id": "jtSVrGAaoOTVzEWT",
            "name": "Header Auth account"
          }
        },
        "onError": "continueRegularOutput"
      },
      {
        "parameters": {
          "assignments": {
            "assignments": [
              {
                "id": "p1",
                "name": "system_prompt",
                "value": "أنت مصنِّف آلي في منصة رصد قانوني داخلية لإدارة قانونية.\n\nمهمتك محدودة بدقة: تلخيص وتصنيف واستخراج بيانات وصفية من نص صادر عن جهة رسمية.\n\nلا تفعل ما يلي إطلاقاً:\n- لا تخترع أي معلومة غير موجودة في النص.\n- لا تفسّر النظام أو تشرح أثره القانوني.\n- لا تستنتج تاريخاً أو رقماً أو جهة غير مذكورة صراحة.\n- لا تعدّل المعنى القانوني أو تعيد صياغته بما يغيّره.\nإن لم تكن المعلومة في النص، أعد null أو قائمة فارغة. الصمت أصح من التخمين.\n\nقرارك الأول: هل هذا تحديث قانوني أو تنظيمي حقيقي؟\n\nاعتبره تحديثاً قانونياً (is_legal_update = true) إذا كان: نظاماً أو قانوناً جديداً، مرسوماً ملكياً أو سلطانياً أو أميرياً، قراراً وزارياً، لائحة تنفيذية، تعميماً، إطاراً تنظيمياً، إشعاراً رسمياً، متطلب امتثال، موعداً تنظيمياً نهائياً، سابقة قضائية، مشروعاً مطروحاً للاستطلاع، تغييراً ضريبياً أو جمركياً، تنظيماً للعمل أو التراخيص أو حماية البيانات أو الأمن السيبراني أو المنافسة أو التنظيم المالي.\n\nاعتبره غير قانوني (is_legal_update = false) إذا كان: خبر مؤتمر أو اجتماع أو ورشة، مذكرة تفاهم، زيارة رسمية، فعالية، مقابلة، بيان صحفي، إحصاءات، تقرير دوري، مقال رأي، محتوى تسويقي أو ترويجي، تعيينات إدارية، أو خبر قديم يعيد نشر ما سبق.\n\nعند الشك، اخفض قيمة confidence بدل أن ترفع is_legal_update. المنصة تتعامل مع الثقة المنخفضة بالرفض، وهذا هو السلوك المطلوب.\n\nأعد كائن JSON واحداً فقط. لا نص قبله ولا بعده، ولا شرح، ولا علامات markdown.\n\n{\"title_ar\":\"\",\"summary_ar\":\"\",\"country\":\"SA|AE|KW|QA|BH|OM|GCC\",\"category\":\"tax|customs|employment|corporate|financial|capital_markets|banking|data_privacy|cybersecurity|competition|intellectual_property|litigation|licensing|real_estate|energy|healthcare|trade|general\",\"document_type\":\"law|royal_decree|ministerial_decision|executive_regulation|circular|regulatory_framework|official_notice|court_precedent|consultation_draft|other\",\"legal_status\":\"enacted|effective|draft|amended|repealed|pending\",\"is_legal_update\":true,\"confidence\":0.98,\"publication_date\":null,\"effective_date\":null,\"affected_entities\":[],\"keywords\":[]}\n\nالقيم المسموحة للحقول المقيّدة محصورة في القوائم أعلاه حرفياً. أي قيمة خارجها تُرفَض ولا تُصحَّح، فاختر من القائمة أو اختر general / other.",
                "type": "string"
              }
            ]
          },
          "includeOtherFields": true,
          "options": {}
        },
        "id": "b413aaa7-4dc4-46fe-bbe0-d139adb1af3c",
        "name": "Classification prompt",
        "type": "n8n-nodes-base.set",
        "typeVersion": 3.4,
        "position": [
          1024,
          160
        ]
      },
      {
        "parameters": {
          "jsCode": "// Carries the RawItem alongside the AI response so the validator can pair\n// them. n8n keeps item order through the HTTP node, but pairing explicitly is\n// cheaper than relying on that.\nconst items = $('Normalise RawItem').all().map(i => i.json);\nreturn $input.all().map((entry, i) => ({ json: { ...entry.json, __item: items[i] } }));"
        },
        "id": "7dbdba25-3c1c-4145-a98d-a87dc926b6a8",
        "name": "Pair with RawItem",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          1328,
          304
        ]
      },
      {
        "parameters": {
          "jsCode": "// Parses and validates the model's JSON. FAIL CLOSED.\n//\n// Anything malformed, incomplete, or outside the allowed enums is rejected\n// outright — never coerced, never defaulted. A coerced category would put a\n// confident wrong classification in a legal archive, which is worse than a gap.\n\nconst COUNTRY = ['SA','AE','KW','QA','BH','OM','GCC'];\nconst CATEGORY = ['tax','customs','employment','corporate','financial','capital_markets','banking','data_privacy','cybersecurity','competition','intellectual_property','litigation','licensing','real_estate','energy','healthcare','trade','general'];\nconst DOC_TYPE = ['law','royal_decree','ministerial_decision','executive_regulation','circular','regulatory_framework','official_notice','court_precedent','consultation_draft','other'];\nconst STATUS = ['enacted','effective','draft','amended','repealed','pending'];\n\nconst ISO_DATE = /^\\d{4}-\\d{2}-\\d{2}$/;\nconst isDate = (v) => {\n  if (v === null || v === undefined || v === '') return true;\n  if (typeof v !== 'string' || !ISO_DATE.test(v)) return false;\n  const d = new Date(v + 'T00:00:00Z');\n  return !Number.isNaN(d.getTime()) && d.toISOString().slice(0,10) === v;\n};\nconst strList = (v) => Array.isArray(v) && v.every(x => typeof x === 'string');\nconst nonEmpty = (v) => typeof v === 'string' && v.trim().length > 0;\n\nconst out = [];\n\nfor (const entry of $input.all()) {\n  const item = entry.json.__item;\n  const response = entry.json;\n\n  const fail = (reason, detail) => out.push({ json: {\n    __item: item, __rejected: true, reason, detail: detail ? String(detail).slice(0,300) : null,\n  }});\n\n  // The Messages API returns content blocks; the text block carries the JSON.\n  const text = response?.content?.[0]?.text;\n  if (!nonEmpty(text)) { fail('ai_empty_response'); continue; }\n\n  let ai;\n  try {\n    ai = JSON.parse(text.trim());\n  } catch {\n    // One tolerated shape: the model wrapped the object in prose or a fence.\n    const m = text.match(/\\{[\\s\\S]*\\}/);\n    if (!m) { fail('ai_parse_failure', text); continue; }\n    try { ai = JSON.parse(m[0]); } catch { fail('ai_parse_failure', text); continue; }\n  }\n\n  if (!ai || typeof ai !== 'object' || Array.isArray(ai)) { fail('ai_not_an_object'); continue; }\n\n  if (!nonEmpty(ai.title_ar)) { fail('ai_invalid_output', 'title_ar'); continue; }\n  if (!nonEmpty(ai.summary_ar)) { fail('ai_invalid_output', 'summary_ar'); continue; }\n  if (!COUNTRY.includes(ai.country)) { fail('ai_invalid_output', 'country=' + ai.country); continue; }\n  if (!CATEGORY.includes(ai.category)) { fail('ai_invalid_output', 'category=' + ai.category); continue; }\n  if (!DOC_TYPE.includes(ai.document_type)) { fail('ai_invalid_output', 'document_type=' + ai.document_type); continue; }\n  if (!STATUS.includes(ai.legal_status)) { fail('ai_invalid_output', 'legal_status=' + ai.legal_status); continue; }\n  if (typeof ai.is_legal_update !== 'boolean') { fail('ai_invalid_output', 'is_legal_update'); continue; }\n  if (typeof ai.confidence !== 'number' || !Number.isFinite(ai.confidence) || ai.confidence < 0 || ai.confidence > 1) {\n    fail('ai_invalid_output', 'confidence=' + ai.confidence); continue;\n  }\n  if (!isDate(ai.effective_date)) { fail('ai_invalid_output', 'effective_date=' + ai.effective_date); continue; }\n  if (!isDate(ai.publication_date)) { fail('ai_invalid_output', 'publication_date=' + ai.publication_date); continue; }\n  if (ai.affected_entities !== undefined && !strList(ai.affected_entities)) { fail('ai_invalid_output', 'affected_entities'); continue; }\n  if (ai.keywords !== undefined && !strList(ai.keywords)) { fail('ai_invalid_output', 'keywords'); continue; }\n\n  out.push({ json: {\n    __item: item,\n    __rejected: false,\n    ai: {\n      title_ar: ai.title_ar.trim(),\n      summary_ar: ai.summary_ar.trim(),\n      country: ai.country,\n      category: ai.category,\n      document_type: ai.document_type,\n      legal_status: ai.legal_status,\n      is_legal_update: ai.is_legal_update,\n      confidence: ai.confidence,\n      publication_date: ai.publication_date || null,\n      effective_date: ai.effective_date || null,\n      affected_entities: (ai.affected_entities || []).slice(0, 20),\n      keywords: (ai.keywords || []).slice(0, 20),\n    },\n  }});\n}\n\nreturn out;"
        },
        "id": "081721b7-6539-42f3-a272-71bf53516dc5",
        "name": "Validate AI output",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          1520,
          304
        ]
      },
      {
        "parameters": {
          "jsCode": "// Builds the string that becomes content_hash, and drops items the AI stage\n// already rejected.\n//\n// Hash inputs: source_id | source_url | title | publication_date. The URL is\n// included deliberately — two authorities republishing the same instrument at\n// different URLs are two archive entries, and the M3 exclusion-group rule\n// handles the mirror case instead.\n\nconst out = [];\nfor (const entry of $input.all()) {\n  const e = entry.json;\n  if (e.__rejected) { out.push({ json: e }); continue; }\n\n  const item = e.__item;\n  const pubDate = item.publication_date || e.ai.publication_date || '';\n\n  out.push({ json: {\n    ...e,\n    hash_input: [item.source_id, item.source_url, e.ai.title_ar, pubDate].join('|'),\n  }});\n}\nreturn out;"
        },
        "id": "5a4585a1-fddb-40a4-a367-c0e4cfa3b01d",
        "name": "Build hash input",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          1728,
          304
        ]
      },
      {
        "parameters": {
          "type": "SHA256",
          "value": "={{ $json.hash_input || 'skipped' }}",
          "dataPropertyName": "content_hash",
          "encoding": "hex"
        },
        "id": "0cfdb2cb-f293-4288-bba3-6fb6a2501b11",
        "name": "SHA256 content hash",
        "type": "n8n-nodes-base.crypto",
        "typeVersion": 1,
        "position": [
          1920,
          304
        ]
      },
      {
        "parameters": {
          "jsCode": "// Prepares the Publishing Gate payload and reads the fail-closed threshold.\n//\n// ai.confidence_threshold is deliberately NOT defaulted here. If it is missing\n// or malformed the gate rejects everything — defaulting it could quietly widen\n// what publishes, so absence must be loud.\n\nconst settings = $('Get threshold').all().map(i => i.json);\nconst row = settings.find(r => r.key === 'ai.confidence_threshold');\nconst threshold = row && typeof row.value === 'number' ? row.value : null;\n\nconst out = [];\nfor (const entry of $input.all()) {\n  const e = entry.json;\n  if (e.__rejected) {\n    // Rejected during AI-response validation: still needs source_id/content_hash\n    // in the same `item` shape the gate expects, or the gate's source re-read\n    // and hash lookup receive \"undefined\" and the Supabase call throws instead\n    // of a clean rejection. The gate short-circuits on __rejected and never\n    // reads `ai`, so it is left null rather than reconstructed.\n    out.push({ json: {\n      __rejected: true,\n      reason: e.reason,\n      detail: e.detail,\n      confidence_threshold: threshold,\n      ai: null,\n      item: {\n        source_id: e.__item.source_id,\n        source_url: e.__item.source_url,\n        content_hash: e.content_hash,\n        publication_date: e.__item.publication_date,\n        raw_excerpt: (e.__item.content_raw || e.__item.title_raw || '').slice(0, 20000),\n        document_path: (e.__item.attachments && e.__item.attachments[0]) || null,\n        ai_model: 'claude-sonnet-5',\n      },\n    }});\n    continue;\n  }\n\n  const item = e.__item;\n  out.push({ json: {\n    __rejected: false,\n    confidence_threshold: threshold,\n    ai: e.ai,\n    item: {\n      source_id: item.source_id,\n      source_url: item.source_url,\n      content_hash: e.content_hash,\n      publication_date: item.publication_date,\n      // Verbatim source text, stored so a lawyer can check the summary against\n      // what the authority actually published.\n      raw_excerpt: (item.content_raw || item.title_raw || '').slice(0, 20000),\n      // The official document URL, preserved end to end.\n      document_path: (item.attachments && item.attachments[0]) || null,\n      ai_model: 'claude-sonnet-5',\n    },\n  }});\n}\nreturn out;"
        },
        "id": "66a59970-b03f-4d7e-8c52-a3367cd7aaf4",
        "name": "Prepare gate payload",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          2128,
          304
        ]
      },
      {
        "parameters": {
          "workflowId": {
            "__rl": true,
            "value": "2AmFW6QzgNwSxaOU",
            "mode": "list",
            "cachedResultUrl": "/workflow/2AmFW6QzgNwSxaOU",
            "cachedResultName": "03 — Publishing Gate"
          },
          "workflowInputs": {
            "mappingMode": "defineBelow",
            "value": {},
            "matchingColumns": [],
            "schema": [],
            "attemptToConvertTypes": false,
            "convertFieldsToString": true
          },
          "options": {
            "waitForSubWorkflow": true
          },
          "mode": "each"
        },
        "id": "249c86c5-e456-4706-a3b2-31c36ffae3ce",
        "name": "Publishing Gate",
        "type": "n8n-nodes-base.executeWorkflow",
        "typeVersion": 1.2,
        "position": [
          2320,
          304
        ]
      },
      {
        "parameters": {
          "jsCode": "// Aggregates every outcome for this source into ONE workflow_logs row.\n//\n// One row per source per run, not one per item: workflow_logs records\n// executions, and the dashboard aggregates from it. Rejections are counted in\n// rejection_reasons rather than logged individually — a rejection is a normal\n// decision, not an incident.\n\nconst src = $('Called by scheduler').first().json.source;\nconst started = $('Called by scheduler').first().json.dispatched_at || new Date().toISOString();\nconst trigger = $('Called by scheduler').first().json.trigger_type || 'scheduled';\nconst correlationId = $('Called by scheduler').first().json.correlation_id || null;\n\nlet fetched = 0, published = 0, rejected = 0;\nconst reasons = {};\nlet errorMessage = null;\n\nfor (const entry of $input.all()) {\n  const e = entry.json;\n  fetched += 1;\n\n  if (e.__rejected) {\n    rejected += 1;\n    reasons[e.reason] = (reasons[e.reason] || 0) + 1;\n    continue;\n  }\n  if (e.outcome === 'published') { published += 1; continue; }\n  if (e.outcome === 'duplicate') { rejected += 1; reasons.duplicate = (reasons.duplicate || 0) + 1; continue; }\n  if (e.outcome === 'rejected') {\n    rejected += 1;\n    reasons[e.reason || 'unknown'] = (reasons[e.reason || 'unknown'] || 0) + 1;\n    continue;\n  }\n  if (e.outcome === 'insert_failed') {\n    rejected += 1;\n    reasons.insert_failed = (reasons.insert_failed || 0) + 1;\n    errorMessage = e.error_message || errorMessage;\n  }\n}\n\nconst finished = new Date().toISOString();\n\n// partial: some items failed to insert. failed is reserved for a run that\n// could not fetch at all, which the M10 failure branch records.\nconst status = errorMessage ? 'partial' : 'success';\n\nreturn [{ json: {\n  workflow_name: '02-source-ingestion',\n  execution_id: String($execution.id),\n  source_id: src.id,\n  trigger_type: trigger,\n  correlation_id: correlationId,\n  status,\n  items_fetched: fetched,\n  items_published: published,\n  items_rejected: rejected,\n  rejection_reasons: reasons,\n  error_message: errorMessage,\n  duration_ms: Math.max(0, new Date(finished).getTime() - new Date(started).getTime()),\n  retry_attempt: 0,\n  started_at: started,\n  finished_at: finished,\n}}];"
        },
        "id": "93bf6454-c769-483b-96b7-6c96535ea99a",
        "name": "Summarise run",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          2528,
          304
        ]
      },
      {
        "parameters": {
          "tableId": "workflow_logs",
          "dataToSend": "autoMapInputData"
        },
        "id": "170a7b3f-ac3f-48af-8f31-93784e1be5bd",
        "name": "Write workflow log",
        "type": "n8n-nodes-base.supabase",
        "typeVersion": 1,
        "position": [
          2720,
          304
        ],
        "credentials": {
          "supabaseApi": {
            "id": "10zDIxzQYFnJuOmh",
            "name": "Supabase account 2"
          }
        }
      },
      {
        "parameters": {
          "conditions": {
            "options": {
              "caseSensitive": true,
              "version": 2
            },
            "conditions": [
              {
                "leftValue": "={{ $json.__empty }}",
                "rightValue": true,
                "operator": {
                  "type": "boolean",
                  "operation": "equals"
                }
              }
            ],
            "combinator": "and"
          },
          "options": {}
        },
        "id": "14acb990-8ec7-4e61-b3f4-76f9560f55a9",
        "name": "Has items?",
        "type": "n8n-nodes-base.if",
        "typeVersion": 2,
        "position": [
          1024,
          304
        ]
      },
      {
        "parameters": {
          "jsCode": "// Deliberate empty-run outcome: the source fetched cleanly but produced\n// nothing normalisable (or everything was filtered by domain/shape). This\n// is a successful run, logged as such, not silently dropped.\nconst src = $('Called by scheduler').first().json.source;\nconst started = $('Called by scheduler').first().json.dispatched_at || new Date().toISOString();\nconst trigger = $('Called by scheduler').first().json.trigger_type || 'scheduled';\nconst correlationId = $('Called by scheduler').first().json.correlation_id || null;\nconst marker = $input.first().json;\nconst finished = new Date().toISOString();\n\nreturn [{ json: {\n  workflow_name: '02-source-ingestion',\n  execution_id: String($execution.id),\n  source_id: src.id,\n  trigger_type: trigger,\n  correlation_id: correlationId,\n  status: 'empty',\n  items_fetched: marker.raw_count || 0,\n  items_published: 0,\n  items_rejected: 0,\n  rejection_reasons: marker.rejected || {},\n  error_message: null,\n  duration_ms: Math.max(0, new Date(finished).getTime() - new Date(started).getTime()),\n  retry_attempt: 0,\n  started_at: started,\n  finished_at: finished,\n}}];\n"
        },
        "id": "74754020-9bb6-4c14-a471-cd1de0ba61cf",
        "name": "Build empty run summary",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          1200,
          460
        ]
      },
      {
        "parameters": {
          "jsCode": "// A source that could not be fetched at all still needs a workflow_logs\n// row (status='failed') and must not leave the webhook response hanging.\nconst started = $('Called by scheduler').first().json.dispatched_at || new Date().toISOString();\nconst trigger = $('Called by scheduler').first().json.trigger_type || 'scheduled';\nconst correlationId = $('Called by scheduler').first().json.correlation_id || null;\nconst e = $input.first().json;\nconst finished = new Date().toISOString();\n\nreturn [{ json: {\n  workflow_name: '02-source-ingestion',\n  execution_id: String($execution.id),\n  source_id: e.source_id,\n  trigger_type: trigger,\n  correlation_id: correlationId,\n  status: 'failed',\n  items_fetched: 0,\n  items_published: 0,\n  items_rejected: 0,\n  rejection_reasons: {},\n  error_message: e.error_message || 'fetch failed',\n  duration_ms: Math.max(0, new Date(finished).getTime() - new Date(started).getTime()),\n  retry_attempt: 0,\n  started_at: started,\n  finished_at: finished,\n}}];\n"
        },
        "id": "05905a1a-1ceb-4fe7-9a0f-c12dd89b980f",
        "name": "Build failed run summary",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          640,
          800
        ]
      },
      {
        "parameters": {
          "jsCode": "// Re-emit the normalised items now that the threshold read (previous\n// node) is guaranteed to have executed. Get threshold has no data\n// dependency on these items -- it only needs to run before 'Prepare gate\n// payload' references it via $('Get threshold'), and a node with no\n// outgoing connection is not guaranteed to run by n8n's execution planner.\nreturn $('Normalise RawItem').all();\n"
        },
        "id": "e5de34d7-1517-43d2-9057-cd27ec9d95fc",
        "name": "Continue after threshold",
        "type": "n8n-nodes-base.code",
        "typeVersion": 2,
        "position": [
          1200,
          160
        ]
      }
    ],
    "connections": {
      "Called by scheduler": {
        "main": [
          [
            {
              "node": "Parser router",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Parser router": {
        "main": [
          [
            {
              "node": "Fetch RSS",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Fetch API",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Fetch HTML",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Fetch PDF index",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Fetch RSS": {
        "main": [
          [
            {
              "node": "Parse XML",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Record fetch failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Parse XML": {
        "main": [
          [
            {
              "node": "RSS → RawItem",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "RSS → RawItem": {
        "main": [
          [
            {
              "node": "Merge lanes",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Fetch API": {
        "main": [
          [
            {
              "node": "API → RawItem",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Record fetch failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "API → RawItem": {
        "main": [
          [
            {
              "node": "Merge lanes",
              "type": "main",
              "index": 1
            }
          ]
        ]
      },
      "Fetch HTML": {
        "main": [
          [
            {
              "node": "Extract listing",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Record fetch failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Extract listing": {
        "main": [
          [
            {
              "node": "HTML → RawItem",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "HTML → RawItem": {
        "main": [
          [
            {
              "node": "Merge lanes",
              "type": "main",
              "index": 2
            }
          ]
        ]
      },
      "Fetch PDF index": {
        "main": [
          [
            {
              "node": "PDF index → RawItem",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Record fetch failure",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "PDF index → RawItem": {
        "main": [
          [
            {
              "node": "Merge lanes",
              "type": "main",
              "index": 3
            }
          ]
        ]
      },
      "Merge lanes": {
        "main": [
          [
            {
              "node": "Normalise RawItem",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Normalise RawItem": {
        "main": [
          [
            {
              "node": "Has items?",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Classification prompt": {
        "main": [
          [
            {
              "node": "Classify with AI",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Classify with AI": {
        "main": [
          [
            {
              "node": "Pair with RawItem",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Pair with RawItem": {
        "main": [
          [
            {
              "node": "Validate AI output",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Validate AI output": {
        "main": [
          [
            {
              "node": "Build hash input",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Build hash input": {
        "main": [
          [
            {
              "node": "SHA256 content hash",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "SHA256 content hash": {
        "main": [
          [
            {
              "node": "Prepare gate payload",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Prepare gate payload": {
        "main": [
          [
            {
              "node": "Publishing Gate",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Publishing Gate": {
        "main": [
          [
            {
              "node": "Summarise run",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Summarise run": {
        "main": [
          [
            {
              "node": "Write workflow log",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Has items?": {
        "main": [
          [
            {
              "node": "Build empty run summary",
              "type": "main",
              "index": 0
            }
          ],
          [
            {
              "node": "Get threshold",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Build empty run summary": {
        "main": [
          [
            {
              "node": "Write workflow log",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Record fetch failure": {
        "main": [
          [
            {
              "node": "Build failed run summary",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Build failed run summary": {
        "main": [
          [
            {
              "node": "Write workflow log",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Get threshold": {
        "main": [
          [
            {
              "node": "Continue after threshold",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Continue after threshold": {
        "main": [
          [
            {
              "node": "Classification prompt",
              "type": "main",
              "index": 0
            }
          ]
        ]
      }
    },
    "authors": "Asayel Qaid",
    "name": null,
    "description": null,
    "autosaved": false,
    "workflowPublishHistory": [
      {
        "createdAt": "2026-08-02T12:25:02.814Z",
        "id": 423,
        "workflowId": "FHt8uKbBcixIWbWO",
        "versionId": "91b93426-c238-453d-9df3-034c466248ac",
        "event": "activated",
        "userId": "c6441430-2935-43cb-a7bd-db0da8ae18ce"
      }
    ]
  }
}
