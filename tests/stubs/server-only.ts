/**
 * Test stub for the `server-only` package, which throws on import by design.
 * Aliased in vitest.config.ts. Next.js resolution is unaffected — the real
 * server/client boundary is still enforced at build time.
 */
export {}
